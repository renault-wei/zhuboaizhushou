# 星辰语音 App · 项目进度看板（To-Do 清单）

> - 本看板是**清单式快照**，随每个里程碑完成同步刷新；事实源 = `docs/DEV-SPRINTS.md` + git 提交记录。
> - 勾选规则：功能包落地并测试通过才打 `[x]`；只验收、不冒进。
> - 更新时间：2026-09-06（商用音色实况批次：开播 + 测试弹幕注入，直播间听到火山音色回复 = G5 语音链路全链路验收通过；
>   弹幕采集路径已定 = B 人工中转；竞品调研存档批次（doc-only）：新增 docs/COMPETITOR-XCAI.md。决策见 §3）
> - 更新时间：2026-09-07（doc-only：定位修订「手机直接开播」+ 竞品硬件情报（USB 回声器 = 声卡桥）+ 三段解耦异步管道架构共识 + 二期手机线真机验证口径，新增 docs/PHONE-LIVE-PLAN.md；决策见 §3 第 11~14 条）
> - 更新时间：2026-09-07（P0 手机线启动批次：工作台就绪态新增「开播前出声自检」引导卡 + 修复折扣文案 `10.0折` 显示 bug，App 测试 89/89 通过；真机出声验收待音频转接线实物到位）
> - 更新时间：2026-09-07（P1 手机线第一步：出声端 SpeechSink 抽象 — 合成与播放分离、本地播放器作为默认 sink 并接管临时 wav 收尾，为远程出声端留缝；本地出声行为不变，服务端测试 119/119）
> - 更新时间：2026-09-07（P1 手机线第二步：远程出声端通道落地 — 新增 `LIVE_SPEAKER_OUTPUT=phone` 开关注入远程出声队列（内存 FIFO），轮询接口 `GET /api/out/speech/next`（登录态鉴权、空队列 204、流式返回 wav 并交付后删临时文件）；`pc` 默认本机播放行为不变，服务端测试 124/124）
> - 更新时间：2026-09-07（P1 手机线第三步：App 助播机出声端 — 轮询远程出声队列并整段串行播放 + 工作台「助播机出声」开关；`flutter analyze` 0 issue、App 测试 95/95 通过）
> - 更新时间：2026-09-07（P1 UI 批次：应用级设计令牌与主题工厂接入 + 现场工作台暗色重排，新增 docs/UI-DESIGN-SPEC.md；flutter analyze 0 issue、App 测试 95/95 通过）
> - 更新时间：2026-09-07（doc-only：内录盒 = AI 主播「声音替身」结论定稿 — 三种接法 + 单机回环待实测，见 docs/PHONE-LIVE-PLAN.md §9；决策见 §3 第 15 条）
> - 更新时间：2026-09-07（doc-only：P-循环台本/P-播出 里程碑方案 v1.1 定稿 — 台本=可复用台本库、场次引用、开播时读快照；正常流程无空台本兜底；默认 6 条/15-80 字/上限 12 条；条间 6s、每轮休 20s；弹幕回复只在空档插入不打断、频控按实际插入时点重算；直播中改台本=手动结束重开生效；新增 docs/LOOP-BROADCAST-PLAN.md；下一步 = 服务端批 A（M1 台本库 CRUD + M2 生成链路））
> - 更新时间：2026-09-07（批量追记：P-话术v1 代码提交（d05f094）、P-循环台本代码两批（M1 台本库 CRUD + M2 一次过审链路 139a4ca、M3 台本库+开播配置绑定 UI a891f40）、克隆录音收敛 2 段念稿（f37e946）、常规页 UI-2 裸色收尾（4facf2e，App 测试 101/101）、公司端任务台控制台 M1~M4 全落地（b9f9cde / c2e2b91 / aa88983 / 69b3924）；服务端测试 171/171、admin build/lint 0 issue。详见 §6 第 18~27 条）
> - 更新时间：2026-09-08（doc-only 定位定稿：产品收敛为「平台无关 AI 语音助播」— 商家任选直播平台开播，App 只做后台 AI 语音（循环台本 + 弹幕问答）；声音出口天然跨平台，平台相关只剩弹幕源（外挂弹幕助手适配）；商业模式 = 直播期间 AI 在线时长 / 轮播计费。新增 docs/PLATFORM-NEUTRAL-VOICE.md）
> - 更新时间：2026-09-08（D 系列结项：真实平台弹幕采集需求关闭（用户拍板），D1/D2.1~D2.3/D5.2 离线交付与调查归档后 D3~D6 不再推进；弹幕源回到「测试弹幕注入优先 + 外挂 / 人工补位」口径，决策见 §6 第 31 条）
> - 更新时间：2026-09-08（P-M5 App 循环播报状态接线：AI 主播卡直播中展示「循环播报中 · 第 x 条 / 第 y 轮」胶囊，未绑台本提示仅弹幕回复；flutter analyze 0 issue、App 测试 103/103，见 §6 第 32 条）
> - 更新时间：2026-09-08（doc-only：公司端计费模型 v0.3 定稿 — 借鉴竞品形态切换为「服务端扫码直充 + 卡密批次核销 + 服务端开关下发」，替换 ¥99/月订阅主路径；docs/console-roadmap.md 增 §1.5 与 §6（M5~M8 批次）；决策见 §6 第 33 条）
> - 更新时间：2026-09-08（M7 商家端收银台落地 — App 收银台页（余额/预充/服务端开关显隐/扫码 mock/卡密核销/流水）+ 控制器 + 测试，commit 链 b2afc5d→6262259→c051828；flutter analyze 0 issue、App 全量 122/122，见 §6 第 35 条）
> - 更新时间：2026-09-08（直播分钟结束结算接线 — /end 即按已播整分钟欠费式结算（先时长余额 → 当月免费直播分钟，缺额仅对账告警不阻断 /end）；新增 liveBilling 服务 + ledger 缺额扣减原语 + 集成测试，服务端全量 294/294，见 §6 第 36 条）
> - 更新时间：2026-09-09（G7 谈单演示示例台本×3 落地 — 台本库「示例台本（谈单演示）」区「套用」直达新建编辑器预填，保存仍走既有落库合规（敏感词扫描不变）；服务端 GET /api/loop-script-samples 只读返回不落库；flutter analyze 0 issue、App 全量 131/131、服务端 307/307，见 §6 第 37 条）
> - 更新时间：2026-09-09（云端部署批次 — 后端上华为云跑通：Node 22 + PostgreSQL 16（19 表）+ ffmpeg 就绪，systemd starvoice.service 常驻，公网 http://113.44.226.189:3000/health 200；locateFfmpeg 兼容 Linux PATH（2465584）；生产 LIVE_TTS_PROVIDER=volc + LIVE_SPEAKER_OUTPUT=phone，云端火山 TTS 冒烟出 wav；App 版本 0.3.0+82 = 云版基线 APK，真机安装待 USB 接入；服务端全量 307/307，见 §6 第 38 条）
> - 更新时间：2026-09-09（火山预设音色 + 纯 AI 语音批次 — 音色拆「克隆/火山预设」两组互斥、prepare 解除实景 mp4 强制，纯 AI 语音可直接就绪开播；云端 v03 迁移已执行、health 200、presets 冒烟返回 Vivi 2.0/云舟 2.0；flutter analyze 0 issue、App 全量 135/135、服务端全量 308/308（31 文件），见 §6 第 39 条）
> - 更新时间：2026-09-09（云版 v0.3.0+83 真机安装冒烟批次 — App 版本升至 0.3.0+83（b210219）出云版基线 APK 并归档 outputs/，adb install 到华为 ELS-AN10 成功（versionCode 82→83）；云端已核实 = 最新 server 代码无需重复部署；App 启动正常、首页保持登录态渲染；UI 走查中 USB 瞬断，待重连续测，见 §6 第 40 条）
> - 更新时间：2026-09-09（UI v2 三 Tab 改造批次 — App 三 Tab 外壳（首页/直播/我的）落地：账号信息、收银台与退出登录收敛到「我的」，首页移除抖音绑定入口由券列表承接；竞品「直播/个人信息」UI 调研归档 docs/ui-v2-research.md；flutter analyze 0 issue、App 全量 138/138，见 §6 第 42 条）
> - 更新时间：2026-09-09（首页「AI 语音开播」主入口批次 — 首页新增状态化主行动卡片（直播中/已就绪直达工作台、空态引导新建、草稿无音色提示去完善）；列表页 idle 草稿按钮「编辑」→「去完善」透传状态；Live 模型补 hasVoiceSource；新增 widget 冒烟 3 例，flutter analyze 0 issue、相邻套件全绿，见 §6 第 41 条）
> - 更新时间：2026-09-09（云端出声修复部署 + 真机出声闭环批次 — liveSpeaker 平台门槛下沉（remoteOutput：平台限制只约束本机出声，phone 远程出声队列任意平台可入队），云端 Linux LIVE_SPEAKER_OUTPUT=phone 恢复出声链路并部署重启；App 版本升至 0.3.0+84 云版包真机复测：现场工作台「助播机出声」开关拉取 /api/out/speech/next 真实播出火山音色 AI 语音（用户听声确认）；服务端 32 文件 311/311，见 §6 第 43 条）
> - 更新时间：2026-09-09（doc-only：竞品低成本语音架构取证落档 + TTS 缓存里程碑立项 — COMPETITOR-XCAI.md 新增 §7（服务端合成下发 mp3 + 播放端缓存复用/历史回放，低成本=结构省钱而非供应商便宜，附 app-service.js 行号取证），PLATFORM-NEUTRAL-VOICE.md 增 §4.1 每小时成本锚点（≤0.4 元/小时、每小时新合成≤2000 字符），新增 docs/LOWCOST-AUDIO-PLAN.md v1（T3 分句缓存→T7 观察项）；未动代码、未调第三方，见 §6 第 44 条）

> - 更新时间：2026-09-09（T3 TTS 分句缓存服务端首查批次 — 新增 tts_audio_cache 表（v04 drizzle 迁移已执行，键=userId+voiceKey+rate+textSha256、命中自增 hit_count、懒清理）+ ttsCache 读写服务 + ttsUsage 真实合成计量埋点（额度原子守门、usage_logs 只记 miss 合成）+ volcTTS 单次调用 speaker/语速覆盖；liveSpeaker.speak 缓存旁路与 loopCaster 首轮预热接线拆为 T4 待办；服务端 typecheck/lint 0、定向 4 文件 29/29、全量 34 文件 321/321，见 §6 第 45 条）
> - 更新时间：2026-09-10（T3 云端部署 + 新云版安装包批次 — ECS 整包对齐本地 HEAD src 重建（修复零散 scp 遗留的 src/env.ts 与旧 sms.ts 类型冲突）、v04 tts_audio_cache 迁移已在云端库执行、重启 /health 200；App 版本升至 0.3.0+85 出云版 APK 归档 outputs/starvoice-app-release-cloud.apk，见 §6 第 46 条）
> - 更新时间：2026-09-10（「我的」页补齐批次 — ProfilePage 收敛「账号与协议 / 帮助与支持」两组：新增钱包时长摘要卡（预充余额 + 本月免费剩余、进 /wallet 返回自刷新、showCharge 控制充值徽标显隐）+ 新增隐私政策 / 用户服务协议 / AI 语音直播说明 / 官方客服 / 关于 5 个静态子页与顶层路由 + app_meta 收敛版本展示；flutter analyze 0 issue、App 全量 141/141，见 §6 第 47 条）

> - 更新时间：2026-09-10（微信收款码接入支付批次 — 收银台扫码弹窗改为展示内置公司微信收款码（真 http(s) 地址仍走服务端下发），充值/到账文案收敛为「微信扫一扫 + 运营人工确权」；flutter analyze 0 issue、App 全量 141/141，真机已装待扫码验收，见 §6 第 48 条）
> - 更新时间：2026-09-10（火山音色库扩容批次 — 预设白名单 2 → 44 条（女 24 / 男 20），修正命名规则为 zh_{gender}_{音色名}_uranus_bigtts，逐条真合成复测 42/44 通过（3 条 1.0 系列 _moon_bigtts 因资源 ID 不匹配剔除）；App 无需改动（预设目录动态拉取）；云端已重建重启并冒烟，见 §6 第 51 条）
> - 更新时间：2026-09-10（档 A 音色接线批次 — ①换音色真生效（live 音色随场次透传 to 火山合成，修复「切音色无变化」）②预设音色真实试听（删 mock 占位，服务端真合成回 wav，克隆音色回落演示音色并如实提示）；克隆真复刻为档 B 付费项，UI 保留「复刻待接入」标注；App 版本 0.3.0+86 → +87；App 全量 144/144、服务端 330/330；真机复验通过（华为 ELS-AN10 / 云版包 0.3.0+87），见 §6 第 52 条）
> - 更新时间：2026-09-10（默认音色批次 — 首页「我的音色」升级为「音色库」列表模式（我的声音 + 预设音色分组列表，可试听 / 可设为默认）+ 新增用户级默认音色（users.default_volc_preset_id，v05 迁移；口径 = 服务端为准 + 本地缓存回落）+ 新建开播配置自动带入默认音色（不动存量场次）；克隆音色「设为默认」推迟到档 B（UI 置灰并给原因）；App 版本 0.3.0+87 → +88；App 全量 148/148、服务端 337/337；云端已部署并真机复验通过（华为 ELS-AN10 / 云版包 0.3.0+88），见 §6 第 53 条）

> - 更新时间：2026-09-10（M9 手机端后台保活批次 — Android 前台服务保活：新增 AssistantKeepAliveService（mediaPlayback 类型 + 常驻低打扰通知 + PARTIAL_WAKE_LOCK + WifiLock）/ MethodChannel `starvoice/keep_alive`（start / stop / isIgnoringBatteryOptimizations / openBatteryOptimizationSettings，Android 13+ 顺带申请通知权限）/ 助播出声启停自动联动保活（失败静默降级不影响出声）/ 首次启用出声弹「透明玻璃引导卡」（电量优化状态 + 一键去放行，仅引导一次不阻断开播）；flutter analyze 0 issue、App 全量 154/154；App 版本 0.3.0+88 → +89，见 §6 第 54 条）

> - 更新时间：2026-09-10（M10 氛围语空档插播 · 服务端批次 — 竞品「氛围语」合规复刻的服务端落地：五类暖场语（欢迎 / 关注 / 点赞 / 报时 / 自定义）模板库 + 分类插播频率设置 + 一键填充默认模板；循环台本句之间的空档由新调度器按优先级插播（出声链路忙 = 弹幕回复优先 → 本轮让位、绝不打断循环句），占位符缺值自动跳过该行（弹幕采集器到位前走无昵称候选，开箱即有暖场声）；服务端 typecheck / lint 0 error、vitest 36 文件 360/360 全绿；App UI 与弹幕采集器（B/C 系列）后置，见 §6 第 55 条）

> - 更新时间：2026-09-11（手机端三项体验批次 — ① 口播语速档：`lives.speech_rate` + 商家滑块 50~100（火山 speech_rate 口径只取加速段），新场次默认固定「很高」=50、开播读一次快照（`services/liveVoice.ts` 的 `clampLiveSpeechRate`）；② 台本生成三场景「骨架复用」：单品卖货 / 到店团购 / 自定义共用同一 system prompt 骨架 + 场景增量片段（`SCENARIO_HINTS`），自定义场景把商家【参考素材】并入 user 且明令未覆盖不编造，scenario 不落库；③ 预设音色试听「方案 A」：试听 wav 预生成静态下发（`services/voicePreview.ts` + `GET /uploads/voice-previews/:file` + `npm run preview:build`），presets 逐条带 previewUrl、App 直连播放、未预生成回落真合成；服务端 typecheck / lint 0 error、215 passed / 154 skipped（本机 PostgreSQL 未运行 → DB 用例 skip）；App 全量 159/159、`dart analyze` No issues；见 §6 第 56 条）
> - 更新时间：2026-09-11（语速档重定标批次 — 真机试听「语速太快」修正：口播语速滑块改火山 speech_rate 口径 0~60、默认 15（≈1.15 倍速，真人带货节奏）；`services/liveVoice.ts` 常量 MIN/MAX/DEFAULT = 0/60/15、`VOLC_TTS_SPEECH_RATE` 兜底 50→15、新增 drizzle `20260911_v08_speech_rate_rescale.sql`（存量 50→15、>60 钳 60），App 滑块区间与文案同步；同句实测 0 档 6.43s / 15 档 5.59s / 25 档 5.35s（旧 50 档 ≈4.3s）；云端已部署 + 44 条试听按新档重生成 + App 0.3.0+91 云版包 + 真机复验；见 §6 第 58 条）
> - 更新时间：2026-09-11（语速二次下调批次 — 真机复听 0.3.0+91 仍反馈「有点太快」：默认档 15 → -10（≈4.7 字/秒，接近真人主播），滑块区间 0~60 放宽到 -20~60；服务端常量 / `VOLC_TTS_SPEECH_RATE` 兜底 / 注释 + 新增 drizzle `20260911_v09_speech_rate_slower.sql`（存量 15→-10）同步；App 滑块与文案、版本 0.3.0+92；云端已部署 + 44 条试听按新档重生成（Vivi 5.62s → 7.44s）+ 真机复验；见 §6 第 59 条）
> - 更新时间：2026-09-11（话术必选 + 去券 + 去准备 + 直播中热更台本批次 — ① 开播配置「绑定话术」改为**必选**（删「不绑定话术」项 + 保存前校验）；② 去掉「绑定团购券」入口（UI/状态/参数/摘要/复播传参全下线，服务端 `coupon_id` 字段保留不动、零迁移）；③ 去掉「开播准备」独立步骤，保存成功即自动 `prepare`（失败不阻断、停留草稿）；④ **直播监控页支持热更话术**：新增 `POST /api/lives/:id/loop-script`（非 live→409、非本人台本→400、空参→400），循环引擎改正为**每轮开头重读台本**（改绑下一轮生效、不打断当前句、清空则只回弹幕）；App 监控页「更新话术」入口；服务端 216 passed / 155 skipped、App 全量 160/160；见 §6 第 60 条）
> - 更新时间：2026-09-11（直播中热更「话术」批次 — 补上第 60 条只热更循环台本的缺口：新增 `POST /api/lives/:id/script` 热更换绑 `scriptId`，非 live → 409、话术不归属 → 400、未过审 → 400 `SCRIPT_NOT_READY`、空参 → 400 `SCRIPT_REQUIRED`；弹幕回复上下文按条实时读取，改绑后**新弹幕立即**用新话术。App 监控页 AI 主播卡拆两个热更入口：「更新话术」= `scriptId`（新弹幕立即生效）/「更新循环台词」= `loopScriptId`（下一轮生效）；服务端 216 passed / 156 skipped、App 全量 161/161；见 §6 第 61 条）
> - 更新时间：2026-09-11（云版部署 + 真机联调验收批次 — ECS 整包对齐本地 HEAD `c453a95` 重建重启（/health 200）+ App 0.3.0+92→+93 出云版 APK（versionCode=93，归档 outputs/starvoice-app-release-cloud.apk）+ 华为 ELS-AN10 真机装包联调：热更话术（`scriptId` 新弹幕立即生效）/ 热更循环台词（`loopScriptId` 下一轮生效、不打断当前句）/ 注入弹幕「how much」AI 回复经 /tmp wav 取证 + 手机拉取播出 / UI「结束直播」按 12 分钟结算转 ended；本批不改业务代码（仅版本号），服务端沿用 216 passed / 156 skipped、App 161/161；见 §6 第 62 条）
> - 更新时间：2026-09-11（单台本多音色修复批次 — 用户报障「一个话本循环里出现多个音色」，定位根因 = 多场并发共享全局单条远程出声队列（无场次维度）→ 音色交错。五项修复：① 同账号开播互斥（已有 `live` 场次 → 409 `LIVE_IN_PROGRESS`）；② 出声队列按 `liveId` 隔离（`GET /api/out/speech/next?liveId=` 只取本场最早一条，不动他场）；③ 场次音色快照统一（开播抓一次冻住，循环口播与弹幕回复共读一份）；④ App 试听与助播出声物理分离（新增 `voicePreviewPlayerProvider`，直播出声中试听直接拦下）；⑤ 云端 5 条历史脏 live 置 ended（备份表 `lives_backup_20260911_stale_live`）。服务端 typecheck / lint 0 error、vitest 377 passed（1 个 `loopScripts.test.ts` 既有失败，改动前即失败）；App analyze No issues、测试 162/162；见 §6 第 63 条）
> - 更新时间：2026-09-11（云端登录放开批次 — 云端加 `SMS_DEV_MODE=true`：生产环境也可在 /api/auth/send-code 响应回传验证码明文、App 自动填入；ECS 整包对齐本地 src 重建重启 /health 200，实测 send-code 回传 `code` + verify-code 登录成功；正式对外前删除该开关；见 §6 第 64 条）
> - 更新时间：2026-09-11（循环台本节奏收紧批次 — 用户反馈「话术句中间隔久、整轮循环休息久，长时间静默会被直播平台判定异常」，拍板条间 6s → 2s、轮间 20s → 6s：服务端 `loopCaster` 默认常量 + 17 处示例台本（`loopScriptSamples`）同步、新增 drizzle `20260911_v10_loop_gap_tighten.sql` 刷存量（5~8s → 2s，NULL 自动跟新默认、>8s 有意保留；本机 UPDATE 7 / 云端 UPDATE 20）；`LOOP-BROADCAST-PLAN.md` 与 App 台本编辑器提示口径同步；云端整包部署重启 /health 200；App 0.3.0+94 → +95 出云版包（versionCode 95 已核实）；服务端 typecheck / lint 0 error、vitest 379 passed（1 个 `loopScripts.test.ts` 既有失败）；App analyze No issues、测试 162/162；见 §6 第 65 条）
> - 更新时间：2026-09-16（doc-only：规格任务梳理归档批次 — 新增 docs/SPEC-TASKS.md（阶段 A/B/C/D 任务表 + §4 三条拍板口径：话术 200 字上限放开 / 欠费只停口播 / 支付保留口子），docs/COMMERCIALIZATION-AUDIT.md 一并入库并销账第 64、65 条遗留；未动代码、未跑测试、未部署，见 §6 第 66 条）
> - 更新时间：2026-09-16（A5-1 火山 TTS 分段合成批次 — 超长话术按标点切段、逐段合成后 ffmpeg 拼装为单段音频、只转码一次、finally 清理中间件；用户话术不删字不截断；新增 4 用例并修 1 处 mock 缺陷；服务端 typecheck / lint 0 error、vitest 37 文件 384/384（0 fail / 0 skip），见 §6 第 67 条）

## 0. 当前大局

| 指标 | 现状 |
|---|---|
| 仓库 | `github.com/renault-wei/zhuboaizhushou`（feature/atmosphere-and-loopCaster） |
| 服务端测试 | ✅ 216 passed / 156 skipped（36 个测试文件；本机 PostgreSQL 未运行 → 需登录 / 需库的用例 skip，**skip ≠ 通过**，起库后应 372 / 372；全量并行偶发 PG 文件锁，用 `npx vitest run --no-file-parallelism` 复跑稳定） |
| 前端静态检查 | ✅ `flutter analyze` No issues found（Flutter 3.47.2 @ `E:\dev\flutter`） |
| 管理后台 | ✅ `npm run build` / `npm run lint` 0 issue（React + AntD，公司端任务台 M1~M4 页面全接通） |
| 最近提交 | 云版部署 + 真机联调验收（见 §6 第 62 条）；直播中热更「话术」（见 §6 第 61 条）；话术必选 + 去券 + 去准备 + 直播中热更台本（见 §6 第 60 条）；语速二次下调：默认 15 → -10、区间放宽 -20~60（≈4.7 字/秒，接近真人主播）+ 云端部署 + 44 条试听重生成（Vivi 5.62s → 7.44s）+ App 0.3.0+92 云版包（见 §6 第 59 条）；语速档重定标 0~60 / 默认 15（≈1.15 倍速）+ 云端部署 + 44 条试听按新档重生成 + App 0.3.0+91 云版包（见 §6 第 58 条）；三项体验批次收尾：试听 44 条预生成（本机 + 云端）+ 云端部署 + App 0.3.0+90 云版包（见 §6 第 57 条）；手机端三项体验：口播语速档 50~100（默认「很高」=50）+ 台本三场景「骨架复用」+ 预设音色试听方案 A（见 §6 第 56 条）；M10 氛围语空档插播 · 服务端（见 §6 第 55 条）；M9 手机端后台保活（见 §6 第 54 条）；默认音色与音色库列表（见 §6 第 53 条）；档 A 音色接线（见 §6 第 52 条） |
| 主推进路线 | v0.2 G 清单（形态已定 = 画面真人出镜 + 后台 AI 语音主播，取消双模式 A/B；托管线 G1→G2 暂停待抖音 key） |
| 产品定位（2026-09-08 定稿） | 平台无关 AI 语音助播：真人 / 实景开播 + 后台 AI 语音；只出声不推流；弹幕 = 外挂适配（口径见 docs/PLATFORM-NEUTRAL-VOICE.md） |
| 当前推进 | 云版部署 + 真机联调验收（见 §6 第 62 条）：ECS 对齐 HEAD 重建重启（/health 200）+ App 0.3.0+93 出云版包 + 华为 ELS-AN10 真机装包联调 —— 热更话术（新弹幕立即生效）/ 热更循环台词（下一轮生效、不打断当前句）/ 弹幕「how much」AI 回复经 /tmp wav 取证并手机播出 / UI 结束直播按 12 分钟结算转 ended，全部通过。遗留：本机未起 PostgreSQL（DB 用例 skip，起库后应 372/372）、克隆音色真复刻仍属档 B、云端历史遗留 live 脏数据待清理、AI 生成话术标题为空时列表回退「未命名话术」（建议补默认标题）。 |

## 1. 能力基线（✅ 已完成，含旧仓库导入部分）

- [x] S0 脚手架：App（Flutter）+ 服务端（Node）+ DB schema / ER 图
- [x] 账号与授权：手机验证码登录（mock 短信）、抖音绑定（mock）、《声音授权协议》签署存档
- [x] 声音链路：录音页（2 段 + 波形 + ≥1min 校验）、克隆任务（CosyVoice，mock 占位）、音色库（列表/试听/删除）
- [x] 话术链路：DeepSeek 生成（提示词内置禁用语清单）+ 命中自动改写 / 安全模板兜底 + 编辑保存（生成成品必然 ready+pass）
- [x] 开播配置：lives CRUD、视频上传、prepare 合成（真实 CosyVoice TTS + ffmpeg 出 mp4，冒烟通过）
- [x] 直播控制接口：一键开播/结束状态机、只读监控、弹幕日志、合规角标恒开
- [x] 客户端页面骨架：登录 / 抖音绑定 / 音色库 / 话术生成与编辑 / 券列表 / 开播列表与表单 / 直播监控页

## 2. v0.2 双模式 G 路线（当前推进主线）

### G1 · 托管模式真实音轨 —— ✅ 完成
- [x] streaming 合成接入真实 CosyVoice TTS（提交 `ed98d6c`）
- [x] ffmpeg 合成冒烟通过
- [ ] 真人口播验收（待 COSYVOICE key，不阻塞代码推进）

### G2 · RTMP 推流 POC —— 🚫 阻塞（等外部资源）
- [ ] 抖音开放平台 key / 企业测试号
- [ ] 验证推流 → 抖音直播间可见
- [ ] 兜底预案：长期不通 → 模式 A 演示退化为本地合成视频预览

### G3 · 弹幕网关 —— 🟡 写入侧完成，路径已定 = B（真实验收待做）
- [x] 统一事件入口 `onDanmaku` + 事件订阅
- [x] `POST /api/lives/:id/danmaku` 写入入口（模拟注入，供联调用）
- [x] `live_danmaku` 落库 + 集成测试
- [x] 方案文档：真实弹幕采集选型（`docs/DANMAKU-CAPTURE-PLAN.md`：B 人工中转 / C 屏幕 OCR 半自动 / D 自用观看端自动，附合规红线）
- [x] 决策：采集路径已定 = **B 人工中转**（2026-09-06 用户选定；零代码、先验收，C / D 二期候选）
- [ ] 真实验收（B）：开播时把直播伴侣弹幕列表的真实弹幕粘贴进工作台「测试弹幕注入」，跑通「采集 → AI → 语音 → 直播间」实况（网关入口已留，业务侧零改动）

### G4 · 实时互动引擎 —— ✅ 完成
- [x] 订阅弹幕 → 加载商家上下文（live + 话术 + 商品快照）
- [x] DeepSeek 生成 1~2 句口播，或 NONE 不回复
- [x] 频控：场次级 5s + 用户级 5s，防刷屏
- [x] 敏感词命中 → 安全兜底话术（不直接念违规内容）
- [x] 出口注入点 `onReply`（G5 在此接 TTS / 播放队列）
- [x] 测试：10 例 + 端到端「POST 弹幕 → 引擎回复」联调
- [x] 接上 G5 播放后做真机验收（模拟弹幕 → 回复出声，用户实测听到）

### G5 · 现场音频链路 —— 🟡 语音链路全通（插话闪避 / 一键接管待续）
- [x] 决策 1：播放端形态 = Windows 后台模式（本机可测，用户已拍板；App 只做控制端）
- [x] 决策 2：声卡方案 = VB-Cable 虚拟声卡注入抖音直播伴侣（驱动已装，CABLE Input 已设为默认播放设备）
- [ ] 决策 3：真人插话识别方式 —— 副机麦克风能量检测（先做手动接管兜底）
- [x] 代码：出声播放器 `voicePlayer.ts`（真实 Windows 播放执行器 + 注入式测试设计）
- [x] 代码：播放队列 + 可打断 + 静音（服务端核心已落地，单测 4 例）
- [x] 冒烟：`npm run audio:smoke` 本机出声通过（提示音占位，非产品口播）
- [x] 环回自检：从 CABLE Output 录制对照 —— 基线静音峰值 -90dB ↔ 播提示音峰值 -29dB，虚拟声卡通路确认
- [x] 代码：`onReply` 临时接「本机语音」出口（`liveSpeaker` = SAPI 合成 + 播放队列，单测 7 例；音色/语速可配，默认 `Microsoft Huihui Desktop`；提交 `70b22ae`）
- [x] 代码：火山 TTS SSE provider + mp3→wav 转码旁路（`volcTTS.ts`，官方 V3 单向流式接口；与 `LocalWavSynth` 同构可注入 `liveSpeaker`；单测 11 例；默认不接现出声链路）
- [x] 代码：火山 TTS key 接线（key 已写入 `.env`；`liveSpeaker` 增加 `LIVE_TTS_PROVIDER=local|volc` 开关，默认 local 保出声；`npm run audio:volc` 冒烟直达火山）
- [x] 代码：火山 TTS 真连通过（✅ 2026-09-06 账号开通后 `npm run audio:volc` 试音成功：火山合成 + 播放 `played`；`LIVE_TTS_PROVIDER=volc` 已切商用音色）
- [ ] 代码：真人插话闪避（真人说话 AI 闭嘴，停止 X 秒后恢复）
- [ ] 代码：一键静音 / 真人接管开关（与 G6 共用）
- [x] 验收（直播伴侣侧）：麦克风选 CABLE Output，彩排音量条随测试音跳动（✅ 2026-09-06 用户实测通过）
- [x] 验收（本机出声）：`liveSpeaker.speak` 冒烟返回 `spoken`，语音经系统默认设备（CABLE Input）播放（2026-09-06）
- [x] 验收（开播实况）：模拟弹幕 → DeepSeek 回复 → 本机语音 → 虚拟声卡 → 直播伴侣/直播间出声（✅ 2026-09-06 用户实测听到）
- [x] 验收（商用音色实况）：开播 + 工作台注入测试弹幕 → AI 回复经火山商用音色 → 虚拟声卡 → 直播伴侣 → 直播间出声（✅ 2026-09-06 用户直播实测听到）

### G6 · 现场直播工作台（真人出镜 + 后台 AI 语音主播）—— 🟡 首批前端落地，后续项待续
- [x] 产品形态拍板：画面真人出镜；后台虚拟 AI 语音主播实时朗读弹幕、介绍产品、回复提问；**不做**双模式 A/B，不加 DB `mode` / `streamType` 字段
- [x] App：直播监控页 →「现场直播工作台」（AppBar 标题 + AI 语音主播状态卡：播报中 / 已停止；合规角标保持恒显）
- [x] App：测试弹幕注入区（输入 → `POST /api/lives/:id/danmaku` → 即时回读弹幕日志与 AI 状态；仅直播中可用，已结束禁用）
- [x] 客户端：`api_client.postDanmaku(id, content)`（返回 `{ danmaku }`）
- [x] 测试桩：`fake_backend` 补齐直播会话状态机与弹幕读写分支（start/end/monitor/danmaku；错误口径镜像服务端 400/404/409）
- [x] 测试：widget 冒烟 3 例（直播中渲染 / 测试弹幕注入并回读 / 已结束禁用发送），`test/live_monitor_test.dart` 3/3 通过
- [x] App：现场工作台 UI 重排（应用级主题令牌 + 固定暗色 workbench，视觉规范与 UI-1~UI-3 批次见 docs/UI-DESIGN-SPEC.md）
- [ ] App：一键开始入口收口到工作台并做真机完整验收（服务端 `start` / `end` RPC 已备；结束按钮已在工作台）
- [ ] App：一键静音 / 真人接管 + 状态扩展（合成中 / 推流中 / AI 运行中 / 真人接管）—— 用户暂缓（「这几步先不弄」），保留后续项
- [ ] 联调：真实弹幕采集（G3 采集侧）接入后跑通「采集 → AI → 语音 → 直播间」全链路实况（当前验收口径 = 工作台测试弹幕注入）

### G7 · 谈单演示 —— 🟡 火锅示例台本×3 落地（贴片/素材包、演示脚本待续）
- [x] 火锅店模板话术
- [ ] 贴片 / 素材包
- [ ] 演示脚本（含报价口径与合规口径）

## 3. 决策点与外部依赖（需要你拍板 / 提供）

1. **G5 播放端形态**：已定 = Windows 后台出声 + VB-Cable 声卡注入直播伴侣（用户拍板）。
2. **G6 双模式落库**：已定 = 不落库。产品形态 = 画面真人出镜 + 后台 AI 语音主播，v0.2 不做托管无人 A 模式，不加 `mode` / `streamType` 字段。
3. **G2 状态**：默认暂停托管线，等抖音 key 后再动；现场线（G3 采集→G5→G6）优先。
4. **需要你提供的资源**：VB-Cable 已装并设为默认播放设备 + 直播伴侣麦克风已选 CABLE Output 且彩排音量条跳动通过（✅）；火山引擎 TTS key 已充值并写入 `.env`，**账号已开通「豆包语音合成大模型 2.0」服务且试音通过**，`LIVE_TTS_PROVIDER=volc` 已切商用音色（✅ 2026-09-06）；个人抖音直播间（真实弹幕自测用，已可用）；抖音开放平台企业认证（仅官方弹幕/推流增强时再申请，非 P0）；若接云端看板：GitHub Personal Access Token（仓库 Contents Read + Projects 读写）。
5. **真实弹幕口径**（用户拍板 2026-09-06）：用**个人直播间**做真实弹幕测试；具体采集路径以第 9 条为准（已定 = B 人工中转），官方企业通道留到上架后增强。
6. **商用 TTS 选型**（用户拍板 2026-09-06）：**火山引擎（豆包语音 TTS）**，key 已充值、账号已开通、试音通过并已切商用音色（✅ 2026-09-06，见第 4 条）；CosyVoice 降为备选；`liveSpeaker` 适配层已隔离，只换 provider。
7. **交付形态**（用户拍板 2026-09-06）：商家**手机 App**（配置 / 控制端）+ 商家 **Windows 客户端**（现场端：语音 + 直播伴侣），两者结合交付。
8. **部署路线**（用户拍板 2026-09-06）：先完成本机自用验收闭环，通过后再规划云 / SaaS。
9. **真实弹幕采集路径**（用户拍板 2026-09-06）：**已定 = B 人工中转**（直播伴侣弹幕列表 → 粘贴进工作台「测试弹幕注入」，零代码先验收）；C 屏幕 OCR 半自动 / D 自用观看端自动读取为二期候选，方案对比与红线见 `docs/DANMAKU-CAPTURE-PLAN.md`。
10. **竞品调研结论**（存档 2026-09-06）：竞品 XCAI/星辰语音 的「弹幕助手」已确认为灰色路线（独立采集器 + 平台名打码 + 抗封/绕声纹检测卖点），不作为功能蓝本；弹幕采集口径维持第 9 条（B → C → 官方通道）。证据与取舍见 `docs/COMPETITOR-XCAI.md`。
11. **交付形态侧重修订**（用户口径 2026-09-07）：**手机直接开播为主线**——目标商家没有电脑、不承担额外工具成本；电脑线（直播伴侣 + 虚拟声卡）降级为开发期自测手段，不作为对外交付形态。定位、约束与形态对比见 `docs/PHONE-LIVE-PLAN.md`。
12. **架构共识**（2026-09-07）：采集端 / AI 大脑 / 出声端 **三段解耦 + 异步管道**，不追求一个程序干完所有事；采集端（模拟观众监听）可独立存在并转发到现有弹幕网关，出声端只认播放队列串行播。详见 `docs/PHONE-LIVE-PLAN.md`。
13. **竞品硬件情报**（调研 2026-09-07）：竞品手机线的「小回声器插手机 USB」本质 = **USB 音频注入盒 / 声卡桥**（把 AI 声音伪装成外接麦克风），只解决声音进直播间、不解决弹幕；弹幕助手生态有成熟成品与开源（清单见手机线方案文档）。
14. **二期手机线真机验证口径**（2026-09-07）：替换原「电脑 + 声卡盒」草案 = **双手机 + 一根音频转接线**，自有抖音号手机开播、后端出声注入开播手机，验收「直播间听到火山音色回复、无回声、可边充边播」；暂不采购声卡。
15. **内录盒结论定稿**（doc-only 2026-09-07）：竞品「小回声器」经用户对实物定位追问后明确 = **USB 内录转换器 / 声卡桥**，机制 = 把 App 播出的 AI 话术伪装成开播手机认得的「外接麦克风」（AI 主播的声音替身）；三种接法 = 电脑出声 / 旧手机助播 App 出声 / 单手机自回环（需盒子支持 loopback，待实测）；对外话术红线不变。详见 `docs/PHONE-LIVE-PLAN.md` §9。

## 4. 工程债

- [x] D1：`docs/db-schema.md` 补 `live_danmaku` 表（G3 前置，已随 G3 提交）
- [ ] D2：敏感词两级词表 + 「最」类误杀治理（按排期放 T19 后台内容审核）——P-话术v1（2026-09-07）已先用「提示词 + 自动改写 + 兜底」缓解生成侧误拦，词库分层仍待 T19
- [x] D3：折扣文案显示 bug（原价≤售价出现 `10.0折`）已修复（`coupon.dart` 无真实优惠不展示折扣），`live_list_page_test` 的 `liveView_*` 断言已随 G6 按钮分支对齐；App 测试 89/89 通过

## 5. 质量基线（每次提交前必过）

- [x] `npm test` → 171/171（17 个测试文件；本机并行偶发 PG 文件锁导致跳过时，`npx vitest run --no-file-parallelism` 复跑即稳定）
- [x] lint / typecheck 0 error
- [x] `flutter analyze` → No issues found
- [x] admin 后台：`npm run build` / `npm run lint` → 0 issue
- [x] 新功能包对应测试补齐后勾选（随 M1~M4 / UI 批次走，App 测试 101/101）

## 6. 下一步建议

1. ✅ G6 首批前端落地：直播监控页 →「现场直播工作台」，AI 语音主播卡 + 测试弹幕注入，`flutter analyze` 0 issue、widget 冒烟 3/3 通过（2026-09-06）。
2. ✅ 不依赖火山 key 批次（本批提交，2026-09-06）：火山 TTS provider 骨架（`volcTTS.ts`，单测 11 例）+ 真实弹幕采集选型文档（`docs/DANMAKU-CAPTURE-PLAN.md`，未动采集代码）。
3. ✅ 火山 TTS 真连：账号开通后 `npm run audio:volc` 试音通过（2026-09-06），`LIVE_TTS_PROVIDER=volc` 已切商用音色；下一步做开播实况复测（直播伴侣 + 工作台测试弹幕，听到的是火山音色即通）。
4. ⏳ 弹幕首轮真实验收（已定 B，语音链路已通只差真实弹幕原文）：开播时让 1~2 位朋友发真实弹幕，把弹幕原文粘贴进工作台「测试弹幕注入」即可复验；C / D 二期候选。
5. ⏳ G6 收尾：一键开始入口收口到工作台 + 真机完整操作一遍（模拟弹幕跑通 G4~G6 的验收口径）。
6. ⏳ 现场增强（G5 收尾）：真人插话闪避 + 一键静音 / 真人接管开关（用户暂缓）。
7. 注意：系统声音仍默认走虚拟声卡，普通音箱无声属正常；需听本地声音时把系统默认播放设备切回扬声器（不影响直播伴侣已选 CABLE Output 的麦克风链路）。
8. ⏳ 真机与打包（交付形态已定 = 手机 App + Windows 客户端）：G6 收尾后出 Android 签名包真机冒烟，再补 Windows 现场端安装与配置引导。
9. ✅ 竞品调研存档（doc-only，2026-09-06）：XCai/星辰语音 弹幕助手 = 灰色路线（证据链）与取舍结论归档 `docs/COMPETITOR-XCAI.md`，未动代码。
10. ⏳ 手机线 P0（已启动，2026-09-07）：真机出声验收 = 后端出声 → 音频转接线 → 开播手机，待转接线实物到位后执行；方案与口径见 `docs/PHONE-LIVE-PLAN.md` §7。
11. ✅ P0 等待期 UI/收尾批次（2026-09-07）：工作台就绪态「开播前出声自检」引导卡（电脑线 / 手机线两条出声路径）+ 折扣文案 bug 修复；`flutter analyze` 0 issue、App 测试 89/89 通过。
12. ⏳ P1 手机线出声端远程化（第一步 SpeechSink 抽象已提交，2026-09-07）：本地出声行为不变；后续实现远程出声端（助播机拉取 / HTTP 轮询），需先拍板网络可达方式与出声端认证。
13. ✅ P1 手机线第二步（后端，2026-09-07）：`LIVE_SPEAKER_OUTPUT=phone` 出声通道 + `GET /api/out/speech/next` 轮询拉取（复用登录鉴权、空队列 204、交付后清理临时 wav；服务端测试 124/124）。留待：Flutter 助播机出声端（轮询拉取并播放）、出声口默认值切 phone 后真机联调；另登记「工作台 UI 美化」待办（2026-09-07 用户反馈界面偏丑）。
14. ✅ P1 手机线第三步（App 助播机出声端，2026-09-07）：服务端出声通道切 `LIVE_SPEAKER_OUTPUT=phone` 时，工作台「助播机出声」开关卡接管 — 轮询 `GET /api/out/speech/next` 拉取待播 wav、整段串行播放（登录态 / 空队静默 / 交付即删对齐服务端口径）；`flutter analyze` 0 issue、App 测试 95/95 通过（新增 fake 出声队列路由、控制器 5 例 + 监控页出声卡 5 例）。留待：出声口切 phone 后真机出声联调；UI 美化待办已登记（沿用第 13 条口径）。
15. ✅ P1 UI 批次（2026-09-07）：app/lib/core/theme/ 设计令牌（星橙 + 现场暗色）+ 主题工厂接入 app.dart（亮/暗跟随系统，workbench 固定深色）；现场工作台重排为暗色操作面板 — 主视觉状态卡（渐变 + 大字时长 + 弹幕胶囊）、AI 语音主播 / 助播机出声 / 开播前自检 / 测试弹幕卡化、弹幕日志圆头像 + 「共 n 条」；flutter analyze 0 issue、App 测试 95/95 通过（UI 视觉规范 = docs/UI-DESIGN-SPEC.md）。留待：常规页 UI-1~UI-3 逐页统一 + 真机视觉走查。
16. ⏳ 手机线硬件定稿（doc-only 2026-09-07）：确认采购 / 申领「内录转换器」实物，按 `docs/PHONE-LIVE-PLAN.md` §3 验证三步 + §9 三种接法实测（重点 = 单手机自回环是否成立）；通过后再回到手机线真机出声联调。
17. ✅ P-话术v1 违禁词一次过生成（2026-09-07，代码已落地待提交）：生成链路升级为「提示词注入禁用语清单（由扫描词表动态生成防漂移）→ 命中自动整段改写（≤2 次，把命中词回传给模型）→ 仍不过退回服务端安全模板（优先带商品字面量，商品本身含极限词时再回退纯通用文案）」；`POST /api/scripts/generate` 成品必然 `ready+pass` 可直接开播，合规红线不变（入库前仍逐字过拦截扫描，编辑保存拦截行为不变）；自动改写 / 兜底时随 201 响应下发一次性 `generationNote`，App 生成页 SnackBar 提示（不入库）；服务端测试 126/126、`flutter analyze` 0 issue、App 测试 95/95。留待：真实 DeepSeek 复测一次生成通过率；下一里程碑规划 = P-循环台本（多集轮播）与 P-播出（ticker 轮询）。
18. ✅ P-话术v1 提交追记（2026-09-07，d05f094）：口径见第 17 条；真实 DeepSeek 通过率复测放话术 UI 联调批次。
19. ✅ P-循环台本 服务端批 A（2026-09-07，139a4ca）：M1 循环台本库 CRUD（归属 / 校验 / 幂等）+ M2 生成链路（复用话术「一次过审」链路，成品必然 pass 可开播）。
20. ✅ P-循环台本 批 B（2026-09-07，a891f40）：M3 循环台本库 + 开播配置绑定 UI（台本列表 / 新建 / 编辑 / 开播表单引用）。
21. ✅ 克隆录音收敛（2026-09-07，f37e946）：录音收敛为 2 段念稿（≥1min 校验保持）+ Windows gradle 兼容修正。
22. ✅ 常规页 UI-2 裸色收尾（2026-09-07，4facf2e）：登录 / 首页 / 音色库 / 券列表 / 录音 / 话术生成与编辑 / 开播列表与表单 / 循环台本 / 抖音绑定共 14 页全部收敛主题令牌与语义色，深浅双模式成立；`flutter analyze` 0 issue、App 测试 101/101。视觉规范状态同步见 docs/UI-DESIGN-SPEC.md §6（UI-1/UI-2 ✅；UI-3 真机视觉走查待手机线批次）。
23. ✅ 公司端任务台控制台（2026-09-07）：里程碑方案 v0.1 建档（07eb7d0，doc-only）+ M1 服务端账号与只读运营接口（b9f9cde，admin JWT 隔离 / bcrypt 登录 / dashboard-merchants-quotas-usage-orders-audit 只读 + seed 脚本，admin 套件 11 例）。
24. ✅ M2 运营写操作（2026-09-07，c2e2b91）：`PUT /api/admin/quotas/:userId` 额度幂等调整 + `POST /api/admin/orders/:id/confirm` 人工确权（订阅顺延 30 天 / 当月付费档刷新），均落 audit_logs 留痕；档位常量独立 `quotaTiers.ts` 供 M4 复用；admin 套件扩至 24 例、全仓 169 例。
25. ✅ M3 管理后台真实接口页面（2026-09-07，aa88983）：React（Vite + AntD）登录页接 `/api/admin/login` + 登录守卫；数据看板 / 商家管理（额度调整抽屉，used 只读保底）/ 算力用量 / 订单订阅（人工确权 Popconfirm）/ 内容审核（三档案 Tab）五页接通真实接口；api.ts 统一 Bearer + 401 登出、adjustQuota 对齐后端局部可空；build / lint 0 issue。
26. ✅ M4 商业化配额接线（2026-09-07，69b3924）：话术生成前按「免费试用档」自动建档 + 预检，额度耗尽 402 `QUOTA_EXCEEDED` 不触发 AI；生成成功才把 `script_generations_used` +1（原子守门）并落一条 usage_logs（category=script_generation / deepseek-chat / costCents=0），失败 502 不扣不记；`server/src/services/quota.ts` 月度账本供后续 TTS / 直播分钟复用；服务端 171/171。
27. ⏳ 公司端后续队列（v0.3 更新）：M5~M8（服务端账本与卡密/扫码单 API、后台卡密批次与开关页、商家端收银台、支付宝当面付真调 — 阻塞）见 docs/console-roadmap.md §6；TTS / 直播分钟扣减接线（复用「时长余额 + quota」账本）排手机线批次；云部署 / SaaS 白标谈单；UI-3 全 App 真机视觉走查随手机线内录转换器批次执行。
28. ✅ 平台无关定位定稿（doc-only，2026-09-08）：定位从「抖音专属」收敛为「平台无关 AI 语音助播」——商家在自选平台开播，App 提供后台 AI 语音（循环台本口播 + 弹幕问答）；声音出口天然跨平台，平台相关只剩「弹幕源」一处（用弹幕助手类成品 / 开源适配转发网关，不自研采集）；商业模式按 AI 在线直播时长 / 轮播量计费，与平台解耦。边界：仍不做多平台推流、不替商家开播，AGENTS 红线不变。新增 docs/PLATFORM-NEUTRAL-VOICE.md，本次不动代码。
29. ✅ D2.5 合规门解锁 + AIOBS 官方 Key 探针（2026-09-08）：用户拍板「开始开发」解锁真实平台采集自研（观众身份、只采自己直播间口径不变）；用官方试用 Key（GUID）真调 `api.aiobs.cn/Douyin/Douyin/SignWss`，服务端返回 `Code:-1 ApiKey is invalid` → 该 GUID 不是 SignWss 可用 ApiKey（预计为桌面端「畅播」试用卡密，详见 docs/COMPETITOR-XCAI.md §6.4）。签名链路需有效 `apikey-dy-*` Web API Key（零售版 Web API 对接 ¥50/月起 / 合伙人档）或改走桌面客户端试用 + helperAdapter 转发路线。
30. ✅ D2.3 抖音采集适配器离线交付（2026-09-08）：纯 TS PushFrame/Response/protobuf 解码（`douyinWire.ts`）+ 可注入签名服务的 wss 适配器（心跳 / ack / 断线 / 归一化 chat·like·enter·gift，`douyinLiveAdapter.ts`）+ 报文字节级 fixture；`createDouyinHttpSigner` 请求体与竞品同构、错误信息带服务端返回体便于排障；新增 `npm run sign:smoke` 手动探针（不烧自动化测试额度）。douyin 适配器单测 18 例全绿，服务端全量 168 passed / 117 skipped；真实链路待有效签名 Key 或桌面客户端路线（见 29）。
31. ✅ D 系列结项（doc-only，2026-09-08）：真实平台弹幕采集需求关闭（用户拍板「弹幕抓取先关闭，其余收尾 / 结项」）。D1 / D2.1~D2.3 / D5.2 离线交付与 AIOBS 调查已归档（797cae9，douyin 适配器单测 35 例全绿），D3~D6 不再推进；对外口径回到「测试弹幕注入优先 + 外挂 / 人工补位、不自研平台采集」，同步 docs/DANMAKU-COLLECTOR-PLAN.md（标记已结项）与 docs/PLATFORM-NEUTRAL-VOICE.md（补落点注记）。自研离线采集代码保留为技术储备。
32. ✅ P-M5 App 工作台循环播报状态（2026-09-08，9b91b0e）：LiveMonitor 补齐 `loopRunning / loopRound / loopCurrentSeq / loopMissing` 解析（服务端 monitor 早已返回）；AI 语音主播卡直播中 = 循环播报中展示「循环播报中 · 第 x 条 / 第 y 轮」胶囊（maxLines 2 防溢出），未绑台本提示「未绑定循环台本（仅弹幕回复）」；fake_backend monitor 补 4 字段 + 新增 2 条 widget 冒烟；`flutter analyze` 0 issue、App 测试 103/103。
33. ✅ 公司端计费模型 v0.3 定稿（doc-only，2026-09-08）：借鉴竞品 xcai1618 形态（服务端收款码 + 轮询对账、卡密批次分销核销、服务端开关下发、无 App 内支付 SDK），商业化主路径从「¥99/月订阅 + 人工确权」切换为「服务端扫码直充 + 卡密批次核销 + 服务端开关下发」；「时长余额 + 月度免费试用档」两本账共存，直播在线分钟先扣余额、回落免费档、再尽 402；真实支付宝收款由 M8 持运营主体凭证接入（红线：本地不真调支付、凭证不硬编码）。docs/console-roadmap.md 增 §1.5 与 §6；AGENTS「不做多档套餐」口径随同批提交同步修订。本次未动业务代码。
34. ✅ M5/M6 公司端账本与后台落地（2026-09-08）：M5 服务端账本（3d1944b→0f7b0e3）——orders.kind/channel/hours 扩展 + card_batches/card_codes/app_config 表（v03 迁移）+ 时长余额入账/原子扣减/流水 ledger + 卡密批次管理/核销/扫码 mock 下单轮询/开关下发 API + 后台订单列表筛选与开关读取带操作人用户名；集成测试 6 例收口（开关下发/扫码确权/卡密核销幂等/时长扣减）。M6 后台页面（f0fca51→d65f0c9，9 提交）——卡密批次管理页（新建批次/导出 txt/批次明细/未核销导出）+ 系统开关配置页（充值显隐/时长档位/公告/扣减优先级，分区保存带更新人）+ 订单页类型/渠道/状态三筛选与订阅/充值标签、pending 充值单「收款码」复看 + 路由与侧边栏接线；admin build/lint 0 issue、服务端 28 文件 291 用例全绿。M8 阻塞于运营主体支付凭证。
35. ✅ M7 商家端收银台落地（2026-09-08，手机线）：v0.3 计费闭环的 App 侧收口。commit 链 b2afc5d（收银台控制器：钱包/开关加载保留旧数据刷新 + 扫码下单/轮询/卡密核销动作，autoDispose 注册）→ 6262259（收银台页：余额与预充分钟卡 + 当月免费配额进度条、服务端开关充值区 showCharge 显隐与 pricePacks 档位、扫码底部 Sheet（mock 收款码 + 查询到账）、卡密兑换表单、充值订单与消费流水列表、错误态内联重试；路由 /wallet + 首页「收银台 / 充值」入口卡）→ c051828（测试：控制器 6 例 + 页面 5 例）。fake_backend 已支持 POST /api/recharge/poll 确权后 paid + balanceMinutes、POST /api/cards/redeem 卡密核销与重复核销 409、failWalletLoad 模拟钱包 500。flutter analyze 0 issue、App 全量 122/122。收银台 UI 测试 Key：walletPage / walletBalanceCard / walletQuotaBar / walletChargeCard / walletRedeemInput 等。后续：M8 真实收款（阻塞，待运营主体支付凭证）；TTS / 直播分钟扣减接线（复用余额 + quota 账本）；真机出声线联调待内录转换器实物。

36. ✅ 直播分钟结束结算接线（2026-09-08，e6672d1）：复用「时长余额 + 免费直播分钟」两本账做直播**结束时**欠费式结算（服务已发生、不引入后台定时器、不改变循环播报行为）。新增 `server/src/services/liveBilling.ts`（`settleLiveSession` + 纯函数 `billableWholeMinutes`：已播整分钟向下取整，起止缺失 / 反向 = 0），`server/src/services/ledger.ts` 补 `drainLiveMinutes` 缺额式扣减原语（按 `app_config.quotaPriority` 先扣时长余额、回落当月免费直播分钟，双双耗尽只计缺额不抛错）；`POST /api/lives/:id/end` 在 `loopCaster.stop` 后接线结算，失败仅告警不阻断 /end。新增集成测试 `server/tests/live_billing.test.ts`（余额优先 / 余额清零回落免费 / 双双耗尽缺额不中断 / 不足 1 分钟不结算 + 纯函数边界）。服务端 lint / tsc 通过、全量 294/294。直播中 402 引导续费闸门（余额耗尽即刻提示、需联动工作台）留后续决策批。
37. ✅ G7 谈单演示示例台本×3 落地（2026-09-09，ba4055e）：服务端内置只读火锅示例（hotpot-set-a/b/c：午市双人 / 四人家庭 / 单人夜宵），GET /api/loop-script-samples 登录即可读、不落库不扣生成配额（集成测试 5/5）；App 台本库新增「示例台本（谈单演示）」区（加载失败可重试、空态隐藏），「套用」直达新建编辑器预填标题与条目（loading 提示 + 标题「套用示例 · 编辑预览」），保存仍走 /api/loop-scripts 既有落库前敏感词扫描，合规红线不变、保存后为独立新台本；fake_backend 补样本路由/故障开关，widget 测试 2 例（套用→预填→保存落库闭环 / 失败重试）；flutter analyze 0 issue、App 全量 131/131、服务端 tsc / lint 通过、全量 307/307。
38. ✅ 云端部署 + 云版 APK 基线（2026-09-09，2465584）：华为云 Flexus（Ubuntu 24.04，公网 113.44.226.189）完成 Node 22 / PostgreSQL 16 / ffmpeg，starvoice 库 19 表 db:push 就位，systemd starvoice.service 常驻、公网 /health 200；修复 locateFfmpeg 用 path.delimiter 切 PATH（兼容 Linux，云端命中 /usr/bin/ffmpeg）；生产 .env 切 LIVE_TTS_PROVIDER=volc + LIVE_SPEAKER_OUTPUT=phone，云端 npm run audio:volc 真连出 wav；App 0.3.0+82 以 API_BASE_URL=http://113.44.226.189:3000 出云版 APK（归档 outputs/starvoice-app-release-cloud.apk），USB 真机安装 + 开播出声复验待手机接入；云密钥仅存本地 .env / server.env 备份，未入库。
39. ✅ 火山预设音色 + 纯 AI 语音就绪（2026-09-09，server 159e906 / app f6dca27）：开播音色新增内置火山预设目录（GET /api/voices/presets 只读，仅 Vivi 2.0 女声 / 云舟 2.0 男声经实测收录，注册在 :id 路由前防吞）；lives 增 volc_preset_id 与 voice_id 互斥归一（服务端白名单校验，越权写入 400 VOLC_PRESET_INVALID，v03 迁移已执行到远端与本库）；prepare 解除「必须先传实景 mp4」强制——有实景视频+就绪克隆音色才走 FFmpeg 压片，纯 AI 语音（无视频或仅预设音色）跳过合成直接置 ready，口播交给直播中出声链路（streaming 集成用例覆盖互斥、白名单与纯 AI ready 不合成）。App 音色选择拆「克隆音色 / 火山预设」两组互斥切换（volcPresetId 与 voiceId 二选一透传），移除实景视频必传引导、支持无视频纯 AI 语音直接开播，列表/工作台摘要区分「（火山预设）」；fake_backend 镜像互斥归一与纯 AI prepare 语义。flutter analyze 0 issue、App 全量 135/135、服务端 typecheck / lint 通过、全量 308/308（31 文件，DB 用例不再 skip）；云端 ECS 已迁移重启，/api/voices/presets 冒烟返回 Vivi 2.0 / 云舟 2.0。
40. ✅ 云版 APK v0.3.0+83 真机安装冒烟（2026-09-09，b210219）：App 版本 0.3.0+82→+83（versionCode 83，旧包可覆盖升级），`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 53.3MB 云版包，归档 outputs/starvoice-app-release-cloud.apk（*.apk 已在 .gitignore，不入库）。SSH 核实云端 /opt/starvoice/server = 最新 server 代码（src 含 volcPresets.ts、schema 含 volc_preset_id、dist 构建 16:49 与 159e906 批次一致，systemd starvoice.service 常驻 /health 200），无需重复部署。华为 ELS-AN10（GMG0220616000176）adb install -r 成功（versionCode 82→83），启动 MainActivity 无崩溃，uiautomator 语义树确认首页已登录（147****9030、声音授权已签署）正常渲染。阻塞项：随后 USB 连接瞬断，开播表单「火山预设音色」两组 UI 走查与出声复验待手机重连后继续。
42. ✅ UI v2 三 Tab 改造（2026-09-09）：App 底部三 Tab（首页 / 直播 / 我的，StatefulShellRoute.indexedStack + AppShell，Keys tabHome/tabLive/tabMe）——首页重排为「品牌头 + AI 直播主行动卡 + 声音克隆区（授权 / 克隆录音 / 我的音色）+ 话术区」三个能力区；抖音绑定入口从首页移除（券列表「去绑定」仍走 /douyin-bind，顶层路由保留）；账号信息 / 收银台 / 声音授权协议 / 退出登录收敛到「我的」Tab（/me，新增 ProfilePage）；/lives、/voices、/scripts、/wallet 等保持全屏路由。首页卡片处于懒加载 ListView 纵深，涉及首页卡片的声音/话术流程用例补 scroll 适配（声音授权用例放大视口规避返回偏移）；douyin 绑定用例改为直达绑定页独立冒烟。竞品「直播 / 个人信息」UI 调研归档 docs/ui-v2-research.md（只抄结构与视觉方向、不搬代码）。flutter analyze 0 issue、App 全量 138/138。
41. ✅ 首页「AI 语音开播」主入口（2026-09-09，8e245fb）：首页顶部原「开播配置」文字卡升级为状态化主行动卡片（主色渐变 + 状态文案）——直播中/已就绪场次直达现场直播工作台 /lives/:id/monitor，空态引导创建 /lives/new，有草稿未绑音色提示「去完善」进列表，从子页返回自动刷新与列表页状态一致；移除含「T11 接入视频源」旧文案（与 v0.3 纯 AI 语音口径一致）。列表页 idle 草稿无音色时按钮「编辑」→「去完善」透传，Live 模型补 hasVoiceSource（voiceId / volcPresetId 任一）判定。新增 widget 冒烟 3 例（空态创建 / 草稿引导 / 就绪直达工作台），flutter analyze 0 issue、相邻套件（clone_voice_entry / live_list / live_monitor）全绿。

43. ✅ 云端出声修复部署 + 真机出声闭环（2026-09-09）：liveSpeaker 平台门槛下沉 — createLiveSpeaker 新增 remoteOutput 判定（env LIVE_SPEAKER_OUTPUT=phone），平台限制只约束「本机出声」（Windows SAPI / 本机播放器），phone 远程出声队列任意平台可入队，云端 Linux 因此可正常合成入远程队列（此前非 Windows 一律 unsupported，云端纯 AI 语音不出声）；配套单测 liveSpeaker.test.ts 覆盖 Linux 本机 unsupported / Windows 本机合成播放 / phone 远程队列 Linux 可入队，既有非 Windows 用例补 remoteOutput:false 显式化。App 版本 0.3.0+83→+84（versionCode 84）出云版 APK（API_BASE_URL=http://113.44.226.189:3000）安装到华为 ELS-AN10 复测。部署：scp liveSpeaker.ts → npm run build → systemctl restart starvoice.service（/health 200，云端 .env LIVE_SPEAKER_OUTPUT=phone）。云端 API 闭环 E2E：Vivi 2.0 预设音色 + 火山 TTS + loopScript + ready-pass 话术 → prepare ready → start live → 远程队列取到 328KB wav → end ended（0 分钟）→ 清理测试场次。真机复测：现场直播工作台「助播机出声（手机线）」开关开启（监听中 · 累计播报递增至 5+ 条），测试弹幕 POST 进日志，手机轮询 /api/out/speech/next 真实播出 AI 语音（循环播报推进至第 3 轮），用户听声确认闭环达成；测试场次「真机出声测试」21:10:59 正常结束。服务端 typecheck / lint 通过、全量 311/311（32 文件）。
44. ✅ doc-only：竞品低成本语音架构取证落档 + TTS 缓存里程碑立项（2026-09-09）— docs/COMPETITOR-XCAI.md 新增 §7（服务端合成/下发 mp3 + 播放端“攒 6 条→顺序播→空了回放 last_audio” = 便宜来源，非供应商便宜，附行号证据，含 /pages/live/new-float、feedback/index-app 等多副本说明）；docs/PLATFORM-NEUTRAL-VOICE.md 增 §4.1（≤0.4 元/小时、每小时新合成≤2000 字符、循环台本首轮预热全缓存命中口径）；新增 docs/LOWCOST-AUDIO-PLAN.md v1（T3 分句缓存→T7 观察项，每里程碑含验收与单测口径）；未动代码、未调用第三方。

45. ✅ T3 TTS 分句缓存服务端首查 + 真实合成计量埋点（代码批，2026-09-09）：新增 `tts_audio_cache` 表（schema.ts 插在 usageLogs 前；v04 drizzle 迁移已执行，唯一索引 = userId+voiceKey+rate+textSha256）+ `ttsCache` 服务（computeTextSha256/countTtsChars/缓存路径解析/查-存-懒清理，命中自增、删行清文件、写失败仅 warn 不阻断）+ `ttsUsage.recordTtsUsage`（月额度原子守门扣 tts_chars_used，成功才写 usage_logs category=tts，超额返回 quota_exceeded）+ volcTTS 单次调用 speaker/speechRate 覆盖（不传时行为不变）；旁路开关 `TTS_CACHE_ENABLED`/`TTS_CACHE_DIR` 走 .env。拆分口径：本批只落服务端基础设施与单测（ttsCache/ttsUsage/volc_tts 定向 21 例），liveSpeaker.speak 缓存旁路与 loopCaster 首轮预热属 T4 接线待办；服务端 typecheck/lint 0、全量 34 文件 321/321（并行偶发 PG 文件锁为已知，单跑稳定）。
46. ✅ T3 云端部署 + 新云版安装包（2026-09-10）：ECS 113.44.226.189 `/opt/starvoice/server` 先备份 src+dist（/root/starvoice-src-bak-20260909.tgz）再整包对齐本地 HEAD（git archive server/src + tsconfig.json 覆盖，修复此前零散 scp 遗留的 src/env.ts 与旧 sms.ts 类型冲突），远端 `npm run build` 0 error；v04 迁移已在云端 PostgreSQL 执行（`tts_audio_cache` 表 + 双索引 + FK 已核实）；`systemctl restart starvoice.service` active、/health 200、dist/config/env.js 确认 ttsCache 开关生效（.env 已加 `TTS_CACHE_ENABLED=true` / `TTS_CACHE_DIR=data/tts-cache`）。App 版本 0.3.0+84→+85（versionCode 85）出云版 APK（API_BASE_URL=http://113.44.226.189:3000，53.5MB）替换归档 `outputs/starvoice-app-release-cloud.apk`；App 代码自 +84 后无变更，本包为对齐 T3 服务端的基线包，真机安装 / 出声验证待 USB 接入。
47. ✅ 「我的」页补齐（App 批，2026-09-10）：对照竞品个人信息区缺口，ProfilePage 重排为「账号与协议 / 帮助与支持」两组 —— 新增直播时长余额摘要卡（预充余额 + 本月免费剩余，点击进 /wallet 返回自动刷新；showCharge 开关下发才展示「去充值」徽标）+ 保留声音授权协议入口；新增 5 个静态子页并注册顶层路由 /profile/privacy、/profile/terms、/profile/ai-info、/profile/support、/profile/about：隐私政策 / 用户服务协议（初稿提示条 + 章节）、AI 语音直播说明（合规说明）、官方客服（客服时段 + 微信号未配置前仅提示待配置，点「复制」给提示不编造号码）、关于星辰语音（logo + 产品简介 + v0.3.0+85 + 版权）；新增 core/config/app_meta.dart 收敛展示名 / 定位 / 版本号（与 pubspec 0.3.0+85 对齐），页脚品牌行 + 版本号。新增 profile_page_test.dart 3 例（钱包摘要 / 客服与关于导航 / 隐私子页初稿提示），widget_test 冒烟放大视口适配「我的」页变高。flutter analyze 0 issue、App 全量 141/141。

48. ✅ 微信收款码接入充值收银台（App 批，2026-09-10）：扫码充值「生成收款码」在服务端下发 mock:// 占位时，扫码弹窗由「占位图标」改为展示内置公司微信收款码（新增 assets/pay/wechat_collect_qr.jpg 资源 + pubspec 注册，Image.asset key=walletScanWechatQrImage）；服务端下发真实 http(s) 收款码时仍走 Image.network，客户端按 _isRealQrUrl 分流。弹窗与充值卡副文案收敛为「请用『微信扫一扫』收款码付款，运营核对到账后入账（一般几分钟）」，「查询到账」提示同步为运营确权口径；wallet.dart 注释同步 mock 通道兜底语义。wallet_page_test 改为断言微信收款码图与提示 key 出现。flutter analyze 0 issue、App 全量 141/141；release APK（0.3.0+85）已 build 并 adb 安装到华为 ELS-AN10，扫码新 UI 待人工肉眼验收（流程：我的 → 收银台 → 预充时长 → 选档生成收款码）。

49. ✅ 助播机出声「常开」记忆（App 批，2026-09-10，Q5）：偏好持久化键 `assistant_speaker_always_on`（SharedPreferences）；现场直播工作台进入即懒加载偏好，场次为直播中（live）时自动拉起助播机出声（无需每次手动开启），手动开关切换写回偏好；助播机控制器 `stop()` 补 `_disposed` 防护、`dispose()` 先停后标记；出声卡片卸载改 `scheduleMicrotask` 异步收口规避 defunct-element 断言。live_monitor_test 新增 Q5 用例（偏好预置 true → 进入 live 监控页自动开启出声，状态「监听中」/开关值 true）并补 SharedPreferences mock 参数；flutter analyze 0 issue、live_monitor 9/9、controller 与 live_list 套件 15/15 通过。

50. ✅ 真机验证（0.3.0+86，App 批，2026-09-10）：华为 ELS-AN10（GMG0220616000176）adb 覆盖安装 versionCode 85→86 成功，逐项走查 —— ① 首页登录态 / 三 Tab / 声音克隆区渲染正常；② 直播 Tab 已结束场次「再来一场」可用（Q2）；③ 编辑开播配置无「实景视频」项、纯 AI 语音「纯 AI 就绪开播」预检直接就绪（Q3）；④ 进入监控 → 开始直播后出现「助播机出声（手机线）」卡，开启为「监听中」，退出列表再进监控自动恢复开启 = 常开记忆生效（Q5）；⑤ 结束直播弹出「本场结算 共结算 1 分钟 · 免费时长抵扣 1 分钟」，免费配额由 20000 分钟扣至 19984（按分钟计费链路）；⑥ 「我的」页余额 / 免费剩余 / 协议子页齐全，关于与页脚版本 0.3.0+86；⑦ 收银台「生成收款码」弹窗正确渲染内置微信收款码（第 48 条待人工验收项闭环）。测试场次与副本已清理，账号回到验证前状态。本批仅版本号 bump（pubspec.yaml / app_meta.dart，+85→+86），未动业务代码；云版 APK 已重出并归档 outputs/starvoice-app-release-cloud.apk。

51. ✅ 火山音色库扩容（服务端批，2026-09-10）：排查「音色只有 2 个」根因 = 白名单过窄 + voice_type 命名猜错（早期按 mars/jupiter/earth 行星轮换猜测，实测全部不匹配）。核实真实命名规则为 zh_{gender}_{音色名}_uranus_bigtts（豆包语音合成模型 2.0，与现有资源 ID seed-tts-2.0 匹配），ICL_ 前缀为客服 / 营销线音色。server/src/services/volcPresets.ts 白名单 2 → 44 条（female 24 / male 20，按「通用带货 / 讲解解说 / 客服营销」分组注释）；server/scripts/volc-voice-catalog-probe.ts 候选清单改为直接引用 VOLC_PRESET_VOICES，保证探测范围与线上白名单永久一致。全量实合成复测 45 条候选 → 42 可用，3 条失败为 1.0 系列（_moon_bigtts：呆萌川妹 / 豫州子轩 / 广州德哥）报 55000000 resource ID is mismatched，需另配 1.0 资源 ID，本目录暂不收录。App 端零改动：voice_library_page / live_form_page 均从 GET /api/voices/presets 动态拉取，服务重启后自动生效；音色不单独收费（按合成字符计费），故音色数量不影响成本。验证：本机 typecheck / lint 0 error；vitest 30 passed | 4 skipped（本机 PostgreSQL 未启动，DB 用例静默 skip，需起 PG 方为全绿）。云端 ECS 113.44.226.189 已同步两文件 + npm run build 0 error + restart，dist 核实 count 44（女 24 / 男 20）、isVolcPresetId 校验通过，真实合成冒烟（zh_male_taocheng_uranus_bigtts）出 wav 正常；/health 200。

52. ✅ 档 A 音色接线修复（App + 服务端批，2026-09-10）：解决用户实测两个现象 —— 「切换音色没有用，永远只有一款音色」+「克隆界面/试听出现 mock 提示」。根因：① 出声链路 `liveSpeaker.speak(text)` 只收文本，`volcTTS` 虽支持 `overrides.speaker` 但全仓零处透传，发音人恒为 `.env` 的 `VOLC_TTS_SPEAKER`；② 音色库试听是 mock 占位 SnackBar，非真实合成。服务端：新增 `services/liveVoice.ts`（`presetSpeechOverrides` 白名单校验 + `loadLiveSpeechOverrides` 按场次解析），`liveSpeaker` 扩签透传 `SpeechOverrides{speaker,speechRate}`，`interactionEngine` 弹幕回复与 `loopCaster` 循环台本均按场次音色合成（空台本早退提前到音色解析前，核心无 DB 依赖，全局单例显式注入加载器）。`volcPresets.ts` 增分组 `VOLC_PRESET_GROUPS`（带货口播 37 / 讲解解说 4 / 客服营销 3）+ `DEFAULT_VOLC_PRESET_ID` + 条目 `group/recommended`（推荐 4 条）+ `findVolcPreset`；`GET /api/voices/presets` 回带 groups/defaultPresetId；新增 `POST /api/voices/preview`（固定演示短句真实合成回 wav，克隆音色回落演示预设并回 `X-Voice-Preview-Fallback` 头，音色非法 400 / 未配 key 503 / 非本人克隆 404；不计额度、不写 usage_logs）。新增 tests/live_voice.test.ts（9 例全 mock 第三方）。App：`voice_library_page` 删 mock 提示、接真实试听（loading 态 + 回落如实告知），克隆 ready 音色标注「复刻待接入（试听为演示音色）」；`live_form_page` 预设音色按分组成员渲染 + 「推荐」标 + 新建自动预选默认音色（用户改选后不回填）；`providers` 抽 `speechOutPlayerProvider` 供试听与助播出声共用；`api_client` 新增 `previewVoice` / `fetchVolcPresetCatalog`。版本 0.3.0+86 → +87（pubspec / app_meta / profile 断言同步）。验证：服务端 typecheck / lint 0 error、vitest 35 文件 330 全绿；App analyze No issues、全量 144/144（新增试听成功 / 试听失败 2 例）。档 B（克隆真复刻，CosyVoice 付费）不在本批范围。真机复验（2026-09-10，华为 ELS-AN10 / 云版包 0.3.0+87）：① 音色库克隆音色标注「复刻待接入（试听为演示音色）」，点「试听」走云端 POST /api/voices/preview 真实合成并出声（无 mock 弹窗；试听中按钮转 loading、提示条如实告知回落演示音色）；② 开播配置列表按场次持久化展示所选预设（voice-switch-check = 解说小明 2.0、2（复播）= 云舟 2.0），证实「切音色真生效」已落库生效；③ 云端冒烟：GET /api/voices/presets 44 条 / 3 分组（带货口播·讲解解说·客服营销）/ 默认 zh_female_vv_uranus_bigtts，同句不同预设合成 wav 哈希不同（320334B vs 328398B），确认发音人随场次透传至火山合成；④ 线上测试场次 voice-switch-check 已按 POST /api/lives/:id/end 正常结束（03:46:20 → 03:49:45）。

53. ✅ 默认音色与音色库列表（App + 服务端批，2026-09-10）：口径 = 服务端为准、本地只做缓存回落（服务端成功响应覆盖本地缓存，缓存读写失败一律不阻断功能）。服务端：users 增 `default_volc_preset_id`（drizzle `20260910_v05_user_default_voice.sql`，本机与云端库均已执行）；`GET /api/voices/presets` 改 async，回 `{presets, groups, defaultPresetId(生效值), userDefaultPresetId(用户设置)}`；新增 `PUT /api/voices/default`（presetId 传 null 即清回系统默认，非法音色 400）；`POST /api/voices/preview` 接 T3 分句缓存（命中回 `X-Voice-Preview-Cache: hit`）；`tests/live_voice.test.ts` 扩 5 例默认音色 + 1 例试听缓存命中。App：新增 `core/storage/voice_cache_store.dart`（SharedPreferences 缓存预设目录 + 默认音色 id，读写异常吞掉不阻断）；`voice_library_controller` 增 `loadCatalog()` / `setDefaultPreset()`（乐观更新 + 失败回滚 rethrow）；新增 `widgets/preset_voice_list.dart`（分组折叠列表 + 试听 / 设为默认）；`voice_library_page` 重写为「默认音色卡 + 我的声音 + 预设音色」三段；`live_form_controller` 拉取失败回落缓存并用缓存默认音色预填（不动存量场次，`_voiceTouched` 守卫生效）；首页入口「我的音色」→「音色库」，副标「x 个声音 · y 个预设」；版本 0.3.0+87 → +88（pubspec / app_meta / profile 断言）。修复：`load()` 由整体重建 state 改为 `copyWith` 只更新 voices（原实现会清空预设目录，真机复现「下拉刷新后预设变空」）。验证：服务端 typecheck / lint 0 error、vitest 35 文件 337 全绿；App `flutter analyze` No issues、全量 148/148。云端：ECS 113.44.226.189 执行 v05 迁移 + 部署 + 重启 /health 200，冒烟 preset 44 条 / setDefault 落库 / preview 二次命中缓存（hit，281166B）/ clearDefault 回系统默认；云版 APK 归档 outputs/starvoice-app-release-cloud.apk。真机复验（华为 ELS-AN10，versionCode 88）：① 首页音色库卡「1 个声音 · 44 个预设」；② 默认音色卡（Vivi 2.0）；③ 预设 3 分组 44 条可展开试听；④ 克隆音色「设为默认」置灰并提示原因；⑤ 设为默认落服务端并即时刷新；⑥ 下拉刷新后目录与默认不丢（验证 load 修复）；⑦ 新建开播配置自动带入默认音色。复验后用户默认音色已 reset 回系统默认。

54. ✅ M9 手机端后台保活（App 批，2026-09-10）：解决「切成后台 / 锁屏后助播出声被系统冻结中断」。取证口径（对照竞品反编译包）：后台能出声的本质 = 进程不被冻结（前台服务 + 唤醒锁 + 定时唤醒），只抄其兼容性 / 合规部分，不抄双进程互拉 / 1 像素 Activity / 隐藏通知 / 静默自启。原生：新增 kotlin `AssistantKeepAliveService`（前台服务 `foregroundServiceType=mediaPlayback` + 常驻低打扰通知（渠道 starvoice_assistant_keep_alive / NOTIFICATION_ID 20260910 / IMPORTANCE_LOW 无声音无振动）+ PARTIAL_WAKE_LOCK（setReferenceCounted(false)）+ WifiLock(WIFI_MODE_FULL_HIGH_PERF)，onDestroy 释放，START_STICKY；companion start / stop）；`MainActivity` 注册 MethodChannel `starvoice/keep_alive`（start / stop / isIgnoringBatteryOptimizations / openBatteryOptimizationSettings；Android 13+ 在 start 时一并申请 POST_NOTIFICATIONS，保证常驻通知可见）；AndroidManifest 增 FOREGROUND_SERVICE / FOREGROUND_SERVICE_MEDIA_PLAYBACK / WAKE_LOCK / POST_NOTIFICATIONS 权限与服务声明（exported=false、stopWithTask=false）。Dart：新增 `core/platform/keep_alive_bridge.dart`（KeepAliveBridge 接口 + MethodChannel 实现 + Noop 实现；非 Android 走空实现，isIgnoringBatteryOptimizations 原生回 null 按已放行处理）+ providers 增 keepAliveBridgeProvider 并注入助手控制器；assistant_speaker_controller 构造加第 4 位可选参数 keepAlive，start → 拉起保活、stop → 释放，全程 try/catch 静默降级（保活失败绝不影响出声）。UI：新增 `features/assistant_speaker/presentation/keep_alive_guide_dialog.dart`（showGeneralDialog + BackdropFilter 背景模糊的透明玻璃卡片：顶部星橙渐变图标块 + 三条要点（切后台不断声 / 持续拉队列 / 随时可关）+ 电量优化状态行 +「暂不设置」/「去放行」双按钮，Fade + Scale 入场）；工作台出声卡首次开启时弹一次（本地键 assistant_keep_alive_guided，仅 Android、不阻断开播），卡片文案补「首次开启请按引导放行后台」。验证：flutter analyze No issues、App 全量 154/154（新增保活控制器 2 例 + 引导弹窗 4 例）。服务端零改动、本批不涉及云端部署；真机切后台 / 锁屏出声不断待硬件到位后复验。

55. ✅ M10 氛围语空档插播 · 服务端（代码批，2026-09-10）：竞品「氛围语」（欢迎 / 关注 / 点赞 / 报时 / 自定义五类暖场短句，循环口播空档自动插播）的合规复刻——本批只做后台 A 系列，App UI（C）与弹幕采集器（B）后置（用户拍板：等弹幕服务 API 到位再动）。**口径**：氛围语=商家级库（复用 `atmosphere_templates`，全场上共用，开播读快照、直播中改不热更、结束重开生效）；不做空库兜底，改用「一键填充默认模板」显式补齐（幂等，只补缺类别）；采集器到位前事件类（欢迎 / 关注 / 点赞）定时插播跳过含 `[昵称]` 的候选行，默认模板每类含 ≥1 条无昵称候选行保证可演示；频率档 welcome/follow/thumb 默认 60s（1–300）、clock 默认 180s（60–3000，有意偏离竞品 60s）、custom 默认 300s（1–3000），0=不插播；优先级 弹幕回复 > 欢迎/关注/点赞 > 报时 > 自定义 > 循环台本句，靠「出声链路忙→本轮让位」实现。**服务端改动**：`services/atmosphere.ts` 扩为纯函数引擎（类别白名单 + 占位符 `[昵称]/[问题]/[答案]/[时间]/[在线人数]` + `{A|B|C}` 随机词 + 频率规则 + 默认模板 + `pickAtmosphereText` 随机起点顺序扫描跳过不可用行，文本上限 200→500 对齐竞品）；新增 `services/atmosphereScheduler.ts`（开播读快照并冻结、`pickDue` 按优先级 + 分类频控取词、`markSpoken` 实际出声后重新计时、`start/stop` 幂等、加载期被 stop 丢弃结果；全局单例供开播启停 + 循环引擎共用）；`services/loopCaster.ts` 注入 `pickAtmosphere/markAtmosphereSpoken/now`，每句台本播完后 `tryInsertAtmosphere`（忙则让位、仅真出声才记账）；新增 `atmosphere_settings` 表（用户级分类频率，唯一索引 user+category）+ drizzle `20260910_v06_atmosphere_settings.sql`（本机库已执行）；新增 `routes/atmosphereSettings.ts`（`GET /api/atmosphere-settings` 拉全五类当前值 + 档位规则 + 占位符 + 优先级，`PUT /api/atmosphere-settings/:category` 校验后 upsert）；`routes/atmosphereTemplates.ts` 增 `POST /api/atmosphere-templates/defaults`（逐类过 `scanSensitive`，命中 blocked 该类不落库）；`routes/lives.ts` 开播 `atmosphereScheduler.start`、结束 `stop`；`app.ts` 注册新路由。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error；`npx vitest run` 36 文件 360/360 全绿（新增 `atmosphere.test.ts` 纯函数 9 例、`atmosphereScheduler.test.ts` 调度器 9 例、`loopCaster.test.ts` 空档插播 4 例、`atmosphereTemplates.test.ts` 路由集成 6 例含默认模板幂等与频率边界）。**遗留**：App 氛围语设置页 / 工作台插播可见性（C 系列）、真实弹幕采集器（B 系列）、云端部署与本机真机复验待下一批。口径见 `docs/ATMOSPHERE-INTERACTION-PLAN.md`。

56. ✅ 手机端三项体验批次（App + 服务端批，2026-09-11）：语速档 50~100 + 台本三场景「骨架复用」+ 预设音色试听「方案 A」。**口径（用户拍板）**：弹窗沿用上批「系统风」不推翻；语速滑块只取加速段 50~100（火山 speech_rate 口径），音色默认语速先固定成「很高」= 50；话本复用 = 骨架复用（三场景共享骨架 + 场景增量片段，scenario 不落库）；试听 = 方案 A（静态预生成直连播放，未预生成回落真合成）。**服务端**：`lives` 增 `speech_rate` integer（NULL = 未设过）+ drizzle `20260911_v07_live_speech_rate.sql`；`services/liveVoice.ts` 增 `MIN_LIVE_SPEECH_RATE=50` / `MAX_LIVE_SPEECH_RATE=100` / `DEFAULT_LIVE_SPEECH_RATE=50` + `clampLiveSpeechRate`（非有限值回落默认、就近取整再钳区间），`presetSpeechOverrides` 改为恒返回语速覆盖项（预设音色与默认音色都吃档位）；`config/env.ts` 兜底 `VOLC_TTS_SPEECH_RATE` 0 → 50（与场次档位口径一致）；`routes/lives.ts` 创建 / 更新读写 speechRate 并钳值；`services/interactionEngine.ts` 弹幕回复合成同口径带档位；`services/loopScript.ts` 拆「骨架 + `SCENARIO_HINTS`(single_product / group_buy / custom) + `buildSystemPrompt`」，自定义场景把 `customBrief` 作为【参考素材】并入 user 并明令未覆盖不得编造；`routes/loopScripts.ts` 校验 scenario 白名单（非白名单 400、缺省按 group_buy）。**试听方案 A**：新增 `services/voicePreview.ts`（演示句 `VOICE_PREVIEW_TEXT` / URL 前缀 `/uploads/voice-previews/` / 文件名白名单 `/^[A-Za-z0-9_-]{1,64}\.wav$/` / `listAvailableVoicePreviewIds` / `resolveVoicePreviewFile`）+ `routes/voices.ts` presets 逐条补 `previewUrl` 且新增无鉴权静态下发 `GET /uploads/voice-previews/:file`（白名单 + `cache-control: public, max-age=86400` + `audio/wav`）+ `config/env.ts` 增 `voicePreview.dir`（默认 `uploads/voice-previews`）+ `scripts/voice-preview-build.ts`（默认 dry-run，`--write` / `--force` / `--only=`）与 `npm run preview:build`。**App**：`Live.speechRate` 与开播配置创建 / 更新透传；开播配置页语速滑块段（默认 50，测试键 `liveSpeechRateSection` / `liveSpeechRateSlider` / `liveSpeechRateValue`）；台本生成页三场景 ChoiceChip + 自定义参考素材输入框；`SpeechOutPlayer` 抽象加 `playUrl` + audioplayers 实现 `_playSource` 复用；音色库试听优先播 `baseUrl + previewUrl`、异常回落 `POST /api/voices/preview`。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error，`npx vitest run` 32 文件通过 + 4 文件 skip、215 passed / 154 skipped / 0 fail（本机 PostgreSQL 未运行 → 需库用例 skip，skip ≠ 通过，起库后应 369 / 369）；App `dart analyze lib test` No issues、`flutter test` 159 / 159（新增语速解析、表单滑块 2 例、试听直连 / 回落 2 例）。**遗留**：（a）本机未起 PostgreSQL，DB 用例待起库复跑全量；（b）`npm run preview:build -- --write` 未执行（脚本默认 dry-run），真跑会调火山合成 44 条 ≈ 0.1 元，属产品动作待用户确认；（c）云端部署与真机复验未做；（d）克隆音色真复刻仍属档 B。

57. ✅ 三项体验批次收尾：试听预生成 + 云端部署（2026-09-11）：① 试听产物生成 —— 先 dry-run 确认清单，再于本机与云端各跑 `npm run preview:build -- --write`，44 条预设音色全部成功（0 失败），产物落 `uploads/voice-previews/{presetId}.wav`（uploads/ 与 *.wav 均不入库）；`VOLC_TTS_SPEECH_RATE` 0 → 50（本机与云端 .env 同改，与场次默认「很高」口径一致，试听即听到线上默认语速）。② 云端部署 —— 华为 ECS 113.44.226.189 `/opt/starvoice/server`：先备份 src + dist（`/root/starvoice-src-bak-20260911.tgz`），再整包对齐本地 HEAD（`git archive HEAD server` 出 tar → 远端解包覆盖，避免零散 scp 漂移），云端库执行 `20260911_v07_live_speech_rate.sql` 并核实 `lives.speech_rate` 列就位，`npm run build` 0 error、`systemctl restart starvoice.service` active、`/health` 200。③ 云端端到端冒烟 —— 造 token 调 `GET /api/voices/presets`：44 条预设、44 条带 `previewUrl`、defaultPresetId = zh_female_vv_uranus_bigtts；静态下发 `GET /uploads/voice-previews/zh_female_vv_uranus_bigtts.wav` 公网 200 / `audio/wav` / 220KB；目录穿越（`..%2f..%2f.env`）与不存在文件名均 404（白名单生效）。④ 云版包 —— App 0.3.0+89 → +90（pubspec / app_meta / profile 断言同步），`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 54.1MB 包（versionCode 90），归档 `outputs/starvoice-app-release-cloud.apk`（*.apk 不入库）。**验证**：本机服务端 typecheck / lint 0 error、vitest 215 passed / 154 skipped；App `dart analyze` No issues、`flutter test` 159 / 159。**遗留**：（a）本机未起 PostgreSQL（DB 用例 skip，起库后应 369 / 369）；（b）无 Android 真机接入（`flutter devices` 仅 Chrome / Edge），真机装 0.3.0+90 复验语速滑块 / 三场景 / 试听待手机接入；（c）克隆音色真复刻仍属档 B。

58. ✅ 语速档重定标：太快 → 真人节奏（App + 服务端批，2026-09-11）：真机装 0.3.0+90 云版包试听后用户反馈「语速有点太快了」—— 旧口径滑块 50~100、默认 50（实测 ≈1.5 倍速，同句 4.32s）。**新口径**：沿用火山 speech_rate 原始区间但只取温和加速段 0~60，**0 = 正常语速，默认 15（≈1.15 倍速）**，10~25 为带货推荐段。**服务端**：`services/liveVoice.ts` 常量 `MIN_LIVE_SPEECH_RATE=0` / `MAX_LIVE_SPEECH_RATE=60` / `DEFAULT_LIVE_SPEECH_RATE=15`；`config/env.ts` 兜底 `VOLC_TTS_SPEECH_RATE` 50 → 15（三处口径统一：未设档 / 场次档 / 试听预生成）；`routes/lives.ts`、`services/live.ts`、`services/interactionEngine.ts`、`db/schema.ts`、`scripts/voice-preview-build.ts` 注释口径同步；新增 `drizzle/20260911_v08_speech_rate_rescale.sql`（幂等两条 UPDATE：旧默认 50 → 15、>60 钳 60；本机与云端库均已执行；实测 34 条存量场次全为 NULL 未设档，故 0 rows affected，符合预期）。**App**：`live_form_page.dart` 滑块区间 0~60 / 默认 15 + 文案改「数值越大口播越快：0 接近真人，默认 15，10~25 适合带货；弹幕回复同速。」；`live.dart` 与 `api_client.dart` 注释同步。**测试适配**：`server/tests/live_voice.test.ts`（显式档 80→45、区间取整用例 72.4→42.4、新增 `clampLiveSpeechRate(100) === MAX`）、`server/tests/lives.test.ts`（创建 72→45、越界 999 → 60、更新 88→55）、`app/test/live_form_page_test.dart`（默认值 15、拖动落库 40、编辑回填 45 且不再越界）、`app/test/live_model_test.dart` 注释。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error、`npx vitest run` 32 文件通过 + 4 skip、215 passed / 154 skipped / 0 fail；App `dart analyze lib test` No issues、`flutter test` 159 / 159。**同句实测档位对照**（演示句 34 字，本机直连火山合成）：rate 0 → 6.43s、rate 15 → 5.59s、rate 25 → 5.35s，旧 rate 50 ≈ 4.3s，确认默认档已从「赶」回到「略快于真人」。**云端部署**：`git archive HEAD server` 出 tar → 覆盖 `/opt/starvoice/server`（先备份 `/root/starvoice-src-bak-20260911-1502*.tgz`）→ 云端库执行 v08 迁移 → `.env` `VOLC_TTS_SPEECH_RATE` 50 → 15（备份 `.env.bak-*`）→ `npm run build` 0 error → `systemctl restart starvoice.service` active → `/health` 200。**试听重生成**：云端 `npm run preview:build -- --write --force` 44/44 成功 0 失败，Vivi 2.0 试听 wav 220110 → 269646 字节（本机音帧校验 24000Hz / mono / 16bit / **5.62s**），公网 `GET /uploads/voice-previews/zh_female_vv_uranus_bigtts.wav` 200；`GET /api/voices/presets` 44 条全带 previewUrl、默认 zh_female_vv_uranus_bigtts。**真机复验**（华为 ELS-AN10 / 云版包 0.3.0+91，versionCode 91 已核实）：① 首页「音色库 1 个声音 · 44 个预设」→ 进音色库；② 点默认音色 Vivi 2.0「试听」→ 云端日志命中新 wav、`dumpsys audio` 显示 starvoice 于 15:06:09.8 起播 → 15:06:15.9 释放（≈6s，与 5.62s 音长吻合），听感「略快于真人、不赶」；③ 新建开播配置页「口播语速」默认 **15**、滑块靠左、提示文案为新口径。**遗留**：（a）本机未起 PostgreSQL（DB 用例 skip，起库后应 369 / 369）；（b）克隆音色真复刻仍属档 B；（c）云端 6 条历史遗留 `live` 状态脏数据（09-09 / 09-10 测试场次）待清理，近 8 小时无 TTS 计费记录、无活跃合成消耗；（d）语速 15 为听感折中值，若仍偏快可直接在滑块下调到 0~10 区间，无需改代码。

59. ✅ 语速二次下调：默认 15 → -10，区间放宽 -20~60（App + 服务端批，2026-09-11）：真机复听 0.3.0+91 后用户再反馈「语速有点太快了」。**新口径**：默认档 `-10`（≈4.7 字/秒，**接近真人主播**），区间由 0~60 放宽到 **-20~60**（负档 = 比真人更舒缓，满足「再慢一点」诉求），10~25 仍是带货偏快档。**服务端**：`services/liveVoice.ts` 常量 `MIN=-20 / MAX=60 / DEFAULT=-10` + 注释标注各档实测字/秒；`config/env.ts` 兜底 `VOLC_TTS_SPEECH_RATE` 15 → -10（未设档 / 场次档 / 试听预生成三处口径统一）；`db/schema.ts`、`services/live.ts`、`services/interactionEngine.ts`、`routes/lives.ts`、`scripts/voice-preview-build.ts` 注释口径同步；新增 `drizzle/20260911_v09_speech_rate_slower.sql`（幂等三条 UPDATE：存量 15 → -10、<-20 钳 -20、>60 钳 60；本机与云端库均已执行，实测三条均 0 rows —— 34 条存量场次全为 NULL 未设档，符合预期）。**App**：`live_form_page.dart` 滑块区间 -20~60 / 默认 -10 + 文案改「数值越大口播越快：默认 -10（接近真人主播），-20 更舒缓；弹幕回复同速。」；`live.dart`、`api_client.dart` 注释同步。**测试适配**：`server/tests/live_voice.test.ts`（下沿 -20 断言、-12.7 → -13 取整）、`server/tests/lives.test.ts`（负档 -12 原样保留、越界下沿 -999 → -20 两例）、`app/test/live_form_page_test.dart`（默认 -10）、`app/test/live_model_test.dart`（samples 45）。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error、`npx vitest run` 32 文件通过 + 4 skip、215 passed / 154 skipped / 0 fail；App `dart analyze lib test` No issues、`flutter test` 159 / 159。**同句实测五档对照**（演示句 34 字，本机直连火山合成）：rate -10 → 7.18s、0 → 6.43s、5 → 5.98s、15 → 5.59s、25 → 5.35s（旧默认 50 ≈ 4.32s），确认新默认档落到「慢于真人、不赶」。**云端部署**（华为 ECS `113.44.226.189` / `/opt/starvoice/server`，systemd `starvoice.service`）：云端库执行 v09 迁移 → `.env` `VOLC_TTS_SPEECH_RATE` 15 → -10（备份 `.env.bak-*`）→ `npm run build` 0 error → 重启 active → `/health` 200。**试听重生成**：云端 `npm run preview:build -- --write --force` 44/44 成功 0 失败，Vivi 2.0 试听 wav 269646 → 357198 字节（**7.44s**，旧 15 档 5.62s），公网静态 200。**App 0.3.0+91 → +92**（pubspec / app_meta / profile 断言同步）：`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 54.1MB 云版包（versionCode 92 已核实），归档 `outputs/starvoice-app-release-cloud.apk`（*.apk 不入库），已装机。**真机复验**（华为 ELS-AN10 / GMG0220616000176 / 云版包 0.3.0+92）：① 音色库点默认音色 Vivi 2.0「试听」→ 云端日志命中新 wav、`dumpsys audio` 显示 15:17:37.813 起播 → 15:17:45.591 释放（≈7.8s，与 7.44s 音长吻合），听感「不赶了」；② 新建开播配置页「口播语速」默认 **-10**、滑块靠左、新文案生效。截图 `outputs/device-tests/v01-home92.png`、`v03-voicelib92.png`、`v04-preview92.png`、`v06-livetab92.png`、`v07-form92.png`。**遗留**：（a）本机未起 PostgreSQL（DB 用例 skip，起库后应 369 / 369）；（b）克隆音色真复刻仍属档 B；（c）云端 6 条历史遗留 `live` 状态脏数据（09-09 / 09-10 测试场次）待清理；（d）-10 为当前听感档，若仍需更慢可在滑块直接拉到 -20，无需改代码。
60. ✅ 话术必选 + 去券 + 去准备 + 直播中热更台本（App + 服务端批，2026-09-11）：用户拍板「绑定话术为必选；直播监控过程中可更新话术」。**① 话术必选**：开播配置页删「不绑定话术」选项（`_scriptId` 不再允许置空）+ 保存前校验（未选 → 提示「请先绑定话术（必选）」不放行）；与既有服务端根因对齐（`POST /api/lives/:id/prepare` 前置要求话术 `status=ready && sensitiveCheckStatus=pass`，否则 400 `SCRIPT_NOT_READY`）。**② 去团购券**：开播配置页删券选择器与券状态、创建/更新不再传 `couponId`、列表卡片摘要删券行、结束场次「再来一场」不再带券参；服务端 `lives.coupon_id` 字段**保留不动**（零迁移），`/api/douyin/coupons` 与券列表页（`coupon_*`）作为独立功能原样保留。**③ 去「开播准备」独立步骤**：删准备区与「合成/准备」按钮，保存成功即自动 `prepare`（`_composeIfDraft`：成功提示「已保存并就绪，进入工作台即可开播」、失败提示「已保存，但自动就绪失败：…」且不阻断、停留草稿）；主按钮文案改「保存并就绪」。**④ 直播中热更台本（核心）**：服务端新增 `POST /api/lives/:id/loop-script`（`services/live.ts#bindLiveLoopScript`：非本人/不存在→404、非 live→409 `LIVE_NOT_LIVE`、台本不存在/不归属→400 `LOOP_SCRIPT_NOT_OWNED`、空参→400 `LOOP_SCRIPT_REQUIRED`；成功后幂等补 `loopCaster.start`）——**只改 `loopScriptId`，不碰 `scriptId`**；`services/loopCaster.ts#runLoop` 重构为**每轮开头重读台本**（首轮用开播快照、轮间休息后 `loadItems` 重读，改绑于下一轮生效、不打断当前句，重读抛错沿用上一轮快照；运行中清空/解绑 → 停止循环只回弹幕）；App 监控页 AI 主播卡在直播中加「更新话术」入口（选我的台本 → 调 /loop-script → 提示「话术已更新，将在下一轮生效」→ 刷 monitor）。**测试**：服务端 `loopCaster.test.ts` 改写 2 例（每轮重读 / 运行中解绑停止）、`live_session.test.ts` 新增 M4 热更用例 + 拆出 `seedLoopScript`；App `fake_backend.dart` 补 `/api/lives/:id/loop-script` 假路由 + 表单/列表/绑定/监控 4 个套件同步（新增监控页「更新话术」路由用例，删券相关断言）。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error、`npx vitest run` 32 文件通过 + 4 skip、**216 passed / 155 skipped / 0 fail**；App `dart analyze lib test` No issues、`flutter test` **160 / 160**。**文档口径**：`docs/LOOP-BROADCAST-PLAN.md` Q6 由「不做热更新」改为「支持热更新、下一轮生效」并同步 §3.2 / §5.1 / §8.2 算法 / §8.4 / §9.1；`docs/ATMOSPHERE-INTERACTION-PLAN.md` Q7 注明氛围语本批未纳入热更。**遗留**：（a）本机未起 PostgreSQL（DB 用例 skip，起库后应 369 / 369）；（b）克隆音色真复刻仍属档 B；（c）云端 6 条历史遗留 live 状态脏数据待清理；（d）本批未部署云端 / 未出真机包（按用户批次节奏，下批统一部署）。
61. ✅ 直播中热更「话术」（App + 服务端批，2026-09-11）：用户拍板「绑定话术为必选 + 直播监控过程中可更新话术」——「必选」已由第 60 条落地，本批补上「热更话术」缺口（上一批只做了热更循环台本 `loopScriptId`，本批补 `scriptId`）。**语义区分**：`scriptId`（话术）= 弹幕回复 AI 上下文，`interactionEngine.ts` 按条 leftJoin 实时读取 → 改绑后**新弹幕立即生效**；`loopScriptId`（循环台词）= loopCaster 轮播台本 → 下一轮开头重读生效。**服务端**：`services/live.ts` 新增 `LiveErrorCode.SCRIPT_NOT_READY` + `isReadyScript`（status=ready 且 sensitiveCheckStatus=pass，复用过审口径）+ `bindLiveScript`（非本人/不存在 → null → 404、非 live → 抛 LIVE_NOT_LIVE → 409、话术不存在/不归属 → SCRIPT_NOT_OWNED → 400、未过审 → SCRIPT_NOT_READY → 400；只改 `scriptId`、不碰 `loopScriptId`）；`routes/lives.ts` 新增 `POST /api/lives/:id/script`（空参 → 400 `SCRIPT_REQUIRED`），`loopScriptStatusCodeOf` 更名 `hotSwapStatusCodeOf` 供两路由共用。**App**：`api_client.dart` 新增 `bindLiveScript`；`live_monitor_page.dart` AI 主播卡直播中改 `Wrap` 双按钮 —— 「更新话术」（新入口，底部弹窗只列 `isReady` 话术、key `liveMonitorScriptOption_{id}`，成功提示「话术已更新，新弹幕将用新话术回复」）+「更新循环台词」（原「更新话术」更名，提示「循环台词已更新，将在下一轮生效」），共用 `_hotSwapButtonStyle()`。**测试**：服务端 `live_session.test.ts` 新增 `seedScript` helper + 用例「热更话术：非直播中 409；直播中换绑 200；参数/归属/过审校验」7 场景；App `fake_backend.dart` 补 `/api/lives/:id/script` 假路由（404/409/400 全镜像）+ `live_monitor_test.dart` 新增「直播中热更话术：选可开播话术后换绑」（断言 blocked 话术不入弹窗）。**零数据库迁移**（scripts/lives 字段均已存在）。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error、`npx vitest run` **216 passed / 156 skipped / 0 fail**（32 文件通过 + 4 skip；skip 155 → 156 因新增 1 个 DB 用例）；App `flutter analyze lib test` No issues、`flutter test` **161 / 161**（160 → 161）。**遗留**：（a）本机未起 PostgreSQL（DB 用例 skip，起库后应 372 / 372）；（b）克隆音色真复刻仍属档 B；（c）云端 6 条历史遗留 live 状态脏数据待清理；（d）本批未部署云端 / 未出真机包。
62. ✅ 云版部署 + 真机联调验收（部署 + 真机验证批，2026-09-11）：用户拍板「打包部署新程序 + 部署云端 + 联通手机真机测试」。**部署**：ECS 整包对齐本地 HEAD `c453a95`（`server-c453a95.tar`，MD5 `4fcbb591ffd79fec3953cac4b43137a4`）→ `npm run build` → `systemctl restart starvoice.service`，`/health` 200、服务 active。**出包**：App `0.3.0+92` → `+93`（`app/pubspec.yaml` + `app/lib/core/config/app_meta.dart`），`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 54.1MB 云版包归档 `outputs/starvoice-app-release-cloud.apk`（aapt2 校验 versionCode=93）；华为 ELS-AN10（Android 12 / GMG0220616000176）`adb install -r` 成功。**联调验收**（场次 `ea9f1509-b33e-4433-98d3-bdd161b90642`，音色小何 2.0）：① 连云 + 登录正常；② 草稿绑话术 → 状态「就绪」；③ 工作台开始直播 → `status=live`、循环播报启动；④ **热更话术**：点「更新话术」换绑 → 服务端 `script_id` 变 `d3b6abb6-…`（`POST /api/lives/:id/script`）；⑤ **弹幕回复（铁证）**：注入弹幕「how much」(16:45:45) → 云端 `/tmp` 秒级新生成 `starvoice-volc-efcf4b2d-….wav`（16:45:47 / 304206 B / 6.336s = AI 回复语音），归档 `outputs/danmaku-reply/reply-howmuch-164547.wav|.mp3`，手机拉取播出成功；⑥ **热更循环台词**：弹窗换绑「火锅团购示例三 · 深夜夜宵」→ 服务端 `loop_script_id` 变 `e65e264f-…`（`POST /api/lives/:id/loop-script`），**不打断当前句、轮次连续推进**（第 8 轮 → 第 10 轮 → 第 11 轮，累计播报 11 → 26 条），`/tmp` 新 wav 时间戳 16:55:19 佐证新台本发声；全程云端日志 0 error。**结束**：UI「结束直播」→ 二次确认「本场结算 · 共结算 12 分钟 · 免费直播时长抵扣 12 分钟」→ 服务端 `status=ended`（`ended_at` 16:51:07）；截图归档 `outputs/device-tests/u11~u18*.png`。**遗留发现**：热更能绑的话术若 `scripts.title` 为空，列表回退显示「未命名话术」（数据形态，建议后续生成链路补默认标题）。**零数据库迁移**；本批不改业务代码（仅版本号），服务端沿用上批 216 passed / 156 skipped、App 161/161。
63. ✅ 单台本多音色修复（App + 服务端批，2026-09-11）：用户报障「试用中发现一个话本里出现多个声音 / 多音色合成」，要求核查。**根因**：远程出声队列 `services/remoteSpeechQueue.ts` 是**全服务唯一一条全局队列**（无场次维度），多场并发（含脏场次残留）时助播机拉取到的是「全局队首」而非本场音频，配音交错 ⇒ 听到多音色。**修复（五项，按用户批准顺序）**：① **开播互斥**：`services/liveSession.ts#startLive` 开播前查同账号 `status='live'` 场次，命中抛 `LIVE_IN_PROGRESS`（`routes/lives.ts` 既有映射 → 409），从源头禁止同账号多场并发；② **出声队列按场次隔离**：`RemoteSpeechJob.liveId`、`push/size/take/clear` 全部带可选 `liveId`（无参 = 旧全局口径兼容），`liveSpeaker` / `remoteSpeechSink` / `loopCaster.speak` 透传场次，路由 `GET /api/out/speech/next` 支持 `?liveId=`（只交付本场最早一条，缺省仍走全局队首）；③ **场次音色快照统一**：`services/liveVoice.ts` 新增 `captureLiveSpeech / getLiveSpeech / forgetLiveSpeech`（开播抓一次冻住，`/start` 抓、`/end` 丢），循环口播（loopCaster `loadVoice`）与弹幕回复（`interactionEngine#onReply`）改为共读同一份快照 —— 修掉「循环读快照、弹幕读库实时值 → 同场两音色」的口径裂缝；④ **App 试听与助播出声分离**：新增 `voicePreviewPlayerProvider`（独立 `AudioplayersSpeechOutPlayer` 实例），音色库三处试听改走它；并加 `_previewAllowed()` 守卫 —— 助播出声启用中（直播中）点试听直接提示「直播出声进行中，已暂停试听（避免混音进直播间）」不发声；⑤ **云端脏数据清理**：ECS 上 2026-09-10 之前的 5 条 `status='live'` 脏场次（弹幕网关测试直播 ×3、实时互动引擎联调直播、火锅店午市循环直播）先整行备份进 `lives_backup_20260911_stale_live` 再置 `ended`（`ended_at = coalesce(started_at, created_at, now())`），清理后仅剩 1 条 09-11 17:18 的真实场次未动。**测试**：服务端新增 6 例 —— `speech_out.test.ts`（按 `liveId` 取件只交付本场最早一条 / 本场空队 204 且不误取他场）、`live_session.test.ts`（同账号第二场 409 `LIVE_IN_PROGRESS`、结束后可再开）、`live_voice.test.ts`（快照抓取后改库不影响本场 / forget 后重取读新值 / 未抓过就地补抓 / 未知脏 id 回落默认音色）；`npm run typecheck` / `npm run lint` 0 error、`npx vitest run` **377 passed / 1 failed**（`loopScripts.test.ts > 生成场景…` 期望 200 实得 502，已用 `git stash` 验证为**改动前既存失败**，属 DeepSeek 生成链路环境问题，不在本批范围）；App `flutter analyze lib test` No issues、`flutter test` **162 / 162**（新增「试听走独立播放器：不占用助播出声通道」1 例，另修 `profile_page_test.dart` 版本号断言 +92 → +93 的既有漂移）。**遗留**：（a）`loopScripts.test.ts` 1 个既有失败（与本批无关，未修）；（b）克隆音色真复刻仍属档 B；（c）云端库缺 `atmosphere_settings` 表（氛围语在云端不生效）；（d）本机全绿需先手动起便携 PostgreSQL（`E:\dev\pgsql`）；（e）本批未部署云端、未出新真机包（待与下批一并部署）。

64. ✅ 云端登录放开（服务端批，2026-09-11）：用户反馈「云端 App 收不到验证码、登不进去」。**根因**：MVP 短信为内存 mock（`services/sms.ts` 随机 6 位，仅 `NODE_ENV !== 'production'` 时回传明文），云端 `NODE_ENV=production` 且未接真实短信通道 ⇒ 任何手机号都拿不到验证码。**改动**：新增独立开关 `SMS_DEV_MODE`（`config/env.ts` 增 `sms.devMode`，`createSmsService` 改为 `env.NODE_ENV !== 'production' || env.sms.devMode`），`.env.example` 补说明（默认 false，正式对外必须关闭，删一行即可关回）；不改 production 语义（/health 仍报 production）。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error；新增 `tests/sms_dev_mode.test.ts` 2 例（生产不回传 / 生产+开关回传），与 `tests/auth.test.ts` 合计 12/12 通过；云端整包对齐本地 src 重建重启 `/health` 200，实测 `send-code` 回传 `code`、`verify-code` 登录成功签发 token。**遗留**：（a）开关为演示口子，正式对外前删云端 `.env` 该行并重启；（b）接真实短信通道（阿里云短信）仍为待办；（c）`docs/COMMERCIALIZATION-AUDIT.md` 待提交。
65. ✅ 循环台本节奏收紧（App + 服务端批，2026-09-11）：用户反馈「话术句中间隔有点久、整轮循环的间隔也有点久，长时间不说话会被直播系统判定有问题」，拍板「条间 6s → 2s、轮间 20s → 6s，存量台本一起刷」。**服务端**：`services/loopCaster.ts` 默认常量 `DEFAULT_ITEM_GAP_SECONDS` 6 → **2**、`DEFAULT_LOOP_REST_SECONDS` 20 → **6**（附中文注释说明修订原因）；`services/loopScriptSamples.ts` 17 处 `gapAfterSeconds`（6/5/8）全量改 2（rg 校验无残留非 2 值）；新增 `drizzle/20260911_v10_loop_gap_tighten.sql`（幂等 `UPDATE loop_script_items SET gap_after_seconds = 2 WHERE gap_after_seconds BETWEEN 5 AND 8`；NULL 条目自动跟随新默认、商家手设 >8s 视为有意保留）。**App**：台本编辑器 hintText（`空=默认2`）与底部说明、`core/models/loop_script.dart` 注释口径 6 → 2 同步；版本 0.3.0+94 → +95（pubspec / app_meta / profile 断言同步）。**文档**：`docs/LOOP-BROADCAST-PLAN.md` Q4 节奏行改 2s/6s 并加修订说明、schema 注释 `null = 用全局默认 2s`。**数据刷新**：本机库 UPDATE 7 行（刷后分布 NULL 10 / 1s 4 / 2s 7 / 3s 1）、云端库 UPDATE 20 行（NULL 10 / 1s 1 / 2s 25 / 3s 1）。**验证**：服务端 `npm run typecheck` / `npm run lint` 0 error、`npx vitest run` **379 passed / 1 failed**（`tests/loopScripts.test.ts > 生成场景…` 期望 200 实得 502，`git stash` 验证为改动前既存失败，属 DeepSeek 生成链路环境问题，不在本批范围）；App `flutter analyze lib test` No issues found、`flutter test` **162 / 162**。**云端部署**：ECS `113.44.226.189`（`/opt/starvoice/server`，systemd `starvoice.service`）整包（src + drizzle）对齐本地重建 → 刷存量 → `npm run build` → 重启 active → `/health` 200。**出包**：`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 54.1MB 云版包（aapt 核实 versionCode 95），归档 `outputs/starvoice-app-release-cloud.apk`（*.apk 不入库）。**节奏量化**：一轮 6 句由 ≈96 秒（静默 ~56s，占 60%）压到 ≈60 秒（静默 ~18s，占 30%）；成本不增（循环台本走 TTS 缓存，缩短间隔只多命中缓存）；副作用是同一组话术每小时重复次数由 ~37 次升到 ~60 次，建议台本用满 8~12 条并打乱顺序对冲平台「重复内容」风控。**遗留**：（a）`loopScripts.test.ts` 1 个既有失败（与本批无关，未修）；（b）静默看门狗（连续静默 12~15s 自动补一句）用户未拍板，未实现；（c）克隆音色真复刻仍属档 B；（d）云端库缺 `atmosphere_settings` 表；（e）`docs/COMMERCIALIZATION-AUDIT.md` 仍待提交；（f）本批新包待真机联调（用户会另说「连接好了」）。

66. ✅ 规格任务梳理归档（doc-only 批次，2026-09-16）：`/spec` 等价流程产物落库 + 商业化补差距审计归档，本批**未动代码、未跑测试、未部署、未出包**。**新增 `docs/SPEC-TASKS.md`**：§1 规格要点（1.1 现状取证带代码锚点——TTS 缓存基础设施已落地但 `liveSpeaker` 未接线 `server/src/services/liveSpeaker.ts:258`、播放后 unlink `:171`、说话人工厂 `:247`/`:298`、音色载入未取 userId/voiceId `server/src/services/liveVoice.ts:52-64`、循环台本 1~12 条 / 单条 1~200 字 `server/src/services/loopScript.ts:9`/`:11`）、§2 四阶段计划（A 无外部依赖可立即开工 / B 需外部资源或真花钱 / C 需租户模型 / D 商业化增强）、§3 阶段 A~D 四张任务表（ID / 任务 / 改动面 / 前置阻塞 / 验收口径 / 状态）、§5 关联文档。**§4 三条拍板口径（2026-09-16）**：（1）**打开 200 字限制** —— 取消 `server/src/routes/loopScripts.ts:94` 硬校验，用户话术不删字 / 不截断 / 不自动切段，AI 侧只留宽松安全上限拦模型异常；风险点 = `server/src/services/volcTTS.ts:271` 整段一次性提交，超长需合成层内部分段 + 拼接为单段音频后再播；（2）**停止口播** —— 欠费（余额 + 免费分钟同时耗尽）时**只停 AI 语音的新合成与播放，画面 / 推流继续**，工作台常驻续费提示；续费到账自动从下一条续播；改动面含 `server/src/services/loopCaster.ts`；（3）**保留口子** —— 本轮不接真实支付，只预留服务端查单接口与订单结构（服务端主动查单 + 订单号金额核对 + 幂等），收款仍走人工转账 / 卡密核销，个人主体资质**不再阻塞**任何任务。**归档**：`docs/COMMERCIALIZATION-AUDIT.md` 随本批入库，**销账**第 64 条遗留（c）与第 65 条遗留（e）「COMMERCIALIZATION-AUDIT.md 待提交」。**待复核（不阻塞开工）**：火山 TTS 单次请求文本长度上限实测值。**遗留**：（a）阶段 A 五项（A1 缓存旁路接线 → A2 `loopCaster` 首轮预热 → A3 用量计量接线 → A4 欠费闸门只停口播 → A5 话术字数放开）均未开工，A1 无前置阻塞；（b）`tests/loopScripts.test.ts` 1 个既存失败（DeepSeek 502，与本批无关，未修）；（c）静默看门狗（连续静默 12~15s 自动补一句）仍待用户拍板；（d）克隆音色真复刻仍属档 B；（e）云端库缺 `atmosphere_settings` 表。
67. ✅ 火山 TTS 分段合成（服务端批，2026-09-16）：落实第 66 条 §4 拍板口径（1）「打开 200 字限制」的**合成层前置改造（A5-1）**。背景 = `VOLC_TTS_MAX_CHARS_PER_REQUEST`（200）实为**火山单次请求上限**，与用户话术 200 字限制无关，整段一次性提交会超限失败。**改动**（`server/src/services/volcTTS.ts`）：新增导出纯函数 `splitTtsSegments(text, maxChars)`——句末标点 `。！？；…!?;` 与换行优先切、次级 `，、,`、无可切点时硬切并避让 UTF-16 代理对，保证切分后按序拼回与原文逐字一致（不丢字 / 不改写 / 不截断）；新增 `VolcMp3Concatenator` 与 `volcDefaultConcatMp3`（ffmpeg concat demuxer 拼装为单段音频，失败抛 `VOLC_TTS_TRANSCODE_FAILED`）；`synthesize` 改为逐段落盘 → 合成 `starvoice-volc-{id}-merged.mp3` → **只转码一次** → `finally` 清理分段与列表等中间件。**测试**：`tests/volc_tts.test.ts` 新增 4 例（切分不变式 / 短文本单请求单转码 / 超长文本逐段请求 + 拼装一次 + 只转码一次 + 中间产物全清 / 拼装失败抛错且无残留）；`tests/loopScripts.test.ts` 修 1 处 mock 缺陷（`mockResolvedValue` 复用同一 Response 致 body 二次消费失败，改 `mockImplementation` 每次返回新 Response）。**验证**：`npm run typecheck`、`npm run lint` 均 0 error；`npx vitest run` **37 passed（37 文件）/ 384 passed（384 用例），0 fail / 0 skip**（销账第 65 条遗留（a）与第 66 条遗留（b）所指的 loopScripts 既存失败，根因 = 测试 mock 复用同一 Response）。**遗留**：（a）**A5-2 未开工**——尚未收起 `server/src/routes/loopScripts.ts` 的 200 字硬校验、手填上限亦未放到 2000 字；（b）火山单次请求文本长度上限仍为估算值 200 字，未做真实超长实测；（c）A1 缓存旁路接线 / A2 首轮循环预热 / A3 用量计量 / A4 欠费闸门与 B 档任务均未开工；（d）USB 内录声卡未到货，手机端出声链路最后一环待验；（e）克隆音色真复刻仍属档 B。

68. ✅ 接手核查 + 弹幕采集专项重启（R0 改范围，doc-only 批次，2026-09-16）：用户拍板「对照反编译的包开发弹幕反应模块」并给出 AIOBS SignWss Web API Key（`apikey-dy-*` 形态），另拍板以 dsh-space 副本为正本、主干收敛、交付边界 = **自用自测**。**① 环境与基线复现**：正本 `main` 已本地快进至真正头（`--ff-only`，远端未推）；`npm` 在本机被执行策略禁（`npm.ps1` 禁止运行脚本）→ 一律走 `npm.cmd`；本机 PostgreSQL **实际在跑**（`E:\dev\pgsql\bin\postgres.exe`，`starvoice` 库在），「本机未起 PostgreSQL」为旧话；新建 `server/.env`（gitignore 覆盖，已被 `check-ignore` 确认）后服务端基线**实测复现 = 37 文件 / 384 用例：383 passed / 1 skipped / 0 failed**，`tsc --noEmit` exit 0。**更正**：本批一度以静态 grep 数出 378 个用例并据此质疑文档，实测为 384，**文档正确、静态计数错误**。1 个 skip = `streaming.test.ts:32` 的 ffmpeg smoke（要求真 ffmpeg 在 PATH），非 DB 用例；故 HANDOVER §9 的「0 skip」表述略微夸大。复现前提 = `.env` 的 `DEEPSEEK_API_KEY` / `VOLC_TTS_API_KEY` **非空**（`script.ts:274` 等为模块级单例，缺变量在 import 期即抛错），单测对 `fetch` 全量 mock，**不真调、不花钱**。**② 需求先决核查结论：该模块已基本建成。** `server/src/collectors/*` 九个文件约 1600 行（`douyinWire` 报文解码 / `douyinLiveAdapter` 含 `createDouyinHttpSigner` / `collectorManager` / `linkResolver` / `helperAdapter` / `simulatorAdapter` / `events` / `types`），且签名器的请求体与用户提供的 `SignWss对接文档` **逐字段同构**（`ApiKey / BrowserName / BrowserVersion / RoomId / UserUniqueId` → 取 `Data.WssUrl`），`.env.example` 早已备好 `DOUYIN_SIGN_*` 三个键位——**零代码改动**。**探针实测 Key 有效**：POST `api.aiobs.cn/Douyin/Douyin/SignWss` 返回 HTTP 200 + `{"Code":0,...,"Data":{"WssUrl":"wss://webcast5-..."}}`，即 `docs/COMPETITOR-XCAI.md` §6.4 写明的「需另购 SignWss 可用的 Web API Key，到手后重跑 `sign:smoke`」**该前置已消除**（当初卡的是 GUID 形态的桌面端激活卡密，实测 `ApiKey is invalid`）。**③ R0 范围翻转**：`docs/DANMAKU-COLLECTOR-PLAN.md` 头部由「已结项 2026-09-08」追加重启声明（**结项原文保留不改写**）+ 新增 **§11 重启批次票表**（R0~R6，**沿用既有 D 编号，不新起一套**——本仓库已有六套并行编号）；`AGENTS.md` 范围行与三方服务表同步（新增 AIOBS 行）；新增 `docs/adr/0003-danmaku-collector-reopened.md`；本批**未动业务代码、未跑测试、未部署、未出包**。**④ 缺口盘点（实测，非转述）**：D0.1/D0.2/D1.1/D2.1/D2.2/D2.3 已完成；**D3.1 未做**（`live_danmaku` 现仅 `live_id` / `sent_at` 两个索引，无 `platform/room_ref/msg_key/msg_type`、无 `(platform,msg_key)` 唯一索引，`collector_watches` 表不存在）；**D3.2 未做**（`collectors/*` 是**零生产调用点的库**——`app.ts` / 15 个路由 / `liveSession.ts` 全不引用，仅测试驱动）；**D5.1 未做**（事件未接 `danmakuGateway.ingest`）。**⑤ 接线关键**：`interactionEngine` **无需改动**——`src/index.ts:12` 已在 `subscribe()`，事件一旦进网关即自动驱动「AI 回复 → 火山 TTS → 出声」既有链路；但 `UnifiedDanmakuEvent` 只带 `liveId` **不带 `userId`**，而 `danmakuGateway.ingest(userId, liveId, …)` 需要 `userId`，故接线层必须自行维护 `liveId → userId` 映射。**遗留**：（a）R1 落库与幂等迁移（本机 + 云端 ECS `113.44.226.189` 都要执行）；（b）R2 采集控制 API 大概率超 AGENTS 第 8 条的 500 行上限，需拆 R2a（service + env）/ R2b（routes + 装配）；（c）R3 真连验收阻塞于**真实房间 ID + 正在开播**；（d）`collector_watches` 表建议按"能砍就砍"不建（`collectorManager.status()` 已提供内存态）；（e）`AGENTS.md` 第 9 行的产品一句话仍是旧形态（「录 3 分钟声音 → AI 克隆音色 → 一键实景无人直播」），与 ADR-0002 的「只出声、平台无关」定位不符，待单独批次修；（f）克隆音色仍无音频管道（`POST /api/voices` 只收 `name/sampleDurationSeconds/sampleFingerprint`，两个实现的 `createCloneTask` 都返回 mock id），README 与 AGENTS 对其表述**远轻于实际**。

69. ✅ 弹幕采集接线落地（R1~R4 代码批，2026-09-16）：把「模块早就写完但没有任何生产调用点」的 `server/src/collectors/*` 真正接到了既有链路上。**R1 落库与幂等**（`8968f22`）：`live_danmaku` 增 `platform / room_ref / msg_key / msg_type` 四列（全可空）+ `(platform, msg_key)` 唯一索引 + `drizzle/20260916_v11_danmaku_collector.sql`（幂等，本机已执行并二次执行验证全 NOTICE 不报错）；`services/danmaku.ts#ingest` 改 `onConflictDoNothing` 并**关键地不二次广播**——重连重放若再广播，G4 引擎会对同一条弹幕重复回复、观众听到两次口播；PG 唯一索引默认 NULLS DISTINCT，故测试注入路径（无 msg_key）多条并存不受影响。**R2 采集控制 API**（`d0850c6`）：新增 `services/liveCollector.ts`（collectors 库的**第一个生产调用点**）——单例 CollectorManager + 适配器按环境装配 + `liveId → userId` 映射（`UnifiedDanmakuEvent` 只带 liveId 不带 userId，而 `danmakuGateway.ingest` 两者都要）+ `onEvent → eventToIngestInput → ingest`；新增 `routes/danmakuSource.ts`（`POST / DELETE / GET /api/lives/:id/danmaku-source`）；`config/env.ts` 增 `douyinSign` 段；`index.ts` 关停时 `dispose()`。**顺带修掉一个静默正确性漏洞**：现成的接线缝 `eventToIngestInput` 只回传 `{content, senderNickname}`，不带 `platform/msgKey` —— 若照原样接线，R1 刚建的唯一索引**永远为 NULL，幂等彻底失效**；该函数已补齐并锁进单测。**R3 事件接线**（`d0850c6` + `8a752ff`）：端到端用例验证「替身采集事件 → **真**落库（含幂等键）→ `interactionEngine` **真**回复」；`interactionEngine` 一行未改（`index.ts:12` 早已 `subscribe()`）。**R4 开播联动与生命周期**（`8a752ff`）：`liveCollector` 拆成配置（`bind` / `stop`）与会话（`resume` / `suspend`）两层；`/start` 调 `resume` 拉起、`/end` 调 `suspend` 停止（保留绑定，下次开播复用同一房间号，同时避免 wss 连接与重连定时器泄漏）；**两边都用 try/catch 包住并只告警——采集起不来（如第三方签名服务不可用）绝不让开播 / 结束失败**，该保证已有专门用例（采集抛错时 `/start` 仍 200）。口径修订：草稿 / 就绪态允许「先把直播间配好」（201 + `running:false`），已结束 / 失败态 409 `LIVE_NOT_COLLECTABLE`。**验证**：`tsc --noEmit` 与 `eslint` 均 0；服务端 **38 文件 / 399 用例：398 passed / 1 skipped / 0 failed**（1 skip = ffmpeg smoke，非 DB 用例）。本批**未部署云端、未出包**。**遗留**：（a）**R5 真连验收阻塞于真实房间 ID + 正在开播**（唯一未验环节）；（b）**防自嗨未做**——报文里其实有发送者稳定 id（`douyinWire.ts` 的 `DouyinUserLite.id`），但 `UnifiedDanmakuEvent` 未透出，需先补字段再与自身账号比对；（c）~~短链仍需展开器~~ **已补**（`collectors/shortLinkExpander.ts` + 注入 `linkResolver`，7 例离线测试）——抖音 App 分享出来的 `v.douyin.com` 短链现在能直接解析出房间号，「用户只给链接」这条路通了；（d）App 侧尚无绑定入口（D4.1），当前用 HTTP 直接起；（e）v11 迁移云端 ECS 待执行；（f）`core.autocrlf=true` 且无 `.gitattributes`，SQL 迁移推 Linux 前需注意换行。

70. ✅ 云端部署 + 真机联调方法论沉淀（R7~R9，2026-09-16）：用户拍板「部署到国内云服务器（通路已有）」并指出「刚才的声音不是从手机发出的」。**① 出声根因**：本地 `.env` 是 `LIVE_SPEAKER_OUTPUT=pc` + `LIVE_TTS_PROVIDER=local` —— 即「在**本机**用 Windows SAPI 合成并就地播放」，根本不会进手机轮询的远程队列；而**云端生产口径本来就是 `volc` + `phone`**，所以「上云」与「修出声」是同一件事的两面。**② R7 采集卡状态轮询**（`app/lib/features/lives/presentation/live_monitor_page.dart`）：此前 `_loadDanmakuSource()` 只挂在「进入页面 / 手动下拉 / 绑定成功」三处，真机实测**绑定成功后卡片一直停在「连接中…」，必须退出重进才更新**；现纳入 `_monitorTimer` 3s 轮询。该 bug 离线测不出、widget 测试也测不出，**只有真机点一遍才会撞上**。**③ R8 云端部署**：`git archive HEAD server` 整包 → `scp` → `/opt/starvoice/` 解包（先备份 `/root/starvoice-bak-20260916-213457.tgz`）；云端执行 `drizzle/20260916_v11_danmaku_collector.sql`（`live_danmaku` 列数 **6 → 10**）；`npm run build` → `systemctl restart starvoice.service` → `/health` 200。**④ 途中踩到三个真坑**：（a）**云端 `.env` 是 CRLF**，`DATABASE_URL` 尾巴带 `\r`，`psql` 报 `database "starvoice\r" does not exist` → `sed -i 's/\r$//'` 归一（这正是 §12.1 那类换行坑的现场版）；（b）**幂等式补键把无效 Key 当成了有效** —— 脚本用 `grep -q '^DOUYIN_SIGN_API_KEY=' .env || echo …` 补键，而云端早已存在一行（值是**旧的无效 GUID `be732746-…`**，即 `COMPETITOR-XCAI` §6.4 探针失败的那把），条件恒真→**直接跳过覆盖**；表现为云端签名 `ApiKey is invalid` 而本地正常。用**指纹比对**（长度 36 vs 46、前缀 `be732746` vs `apikey-dy`）定位，`sed` 覆盖为可用 Key 后签名通过。**教训：「键存在」≠「键正确」**，凭据部署必须校验值而不是校验存在性；（c）**既有 `sign:smoke` 只从命令行第三参取 `UserUniqueId`、不读 `.env`**，只传房间号会报 `UserUniqueId is required.` —— 极易误判成 Key 无效；已修为「命令行 → `.env` 回落」。**⑤ 云端验证**：`sign:smoke` 签名成功（285ms）；`collect:smoke` 真连 **`status: connected`**、`openAttempts 1`、ttwid 已带上（0 事件 = 房间当时无 `chat`/gift/enter，与之前结论一致）。**⑥ R11 云版 APK**：`flutter build apk --release --dart-define=API_BASE_URL=http://113.44.226.189:3000` 出 54.2MB，`adb install -r` **Success**，已归档 `outputs/starvoice-app-release-cloud-20260916.apk`。**⑦ 真机联调方法论沉淀**（`adb screencap` 读图 → `input tap/text` → 截图 + 服务端日志交叉核对）：本次首次用 adb 驱动真机跑通「进工作台 → 点开始直播 → 贴分享链接 → 开始采集 → 卡片显示『抖音 · 房间 7686117195721837352 · 监听中』」，**证明「我不能操控手机」是错误判断**；两个坑：Flutter 默认不向 `uiautomator` 暴露控件（dump 只有 6 个空容器，必须看图点坐标）、`keyevent 4` 在键盘已收起时会弹掉整个页面。**遗留**：（a）R9 手机出声待云版真机复验（需真实弹幕触发）；（b）R10 防自嗨未做（需给 `UnifiedDanmakuEvent` 补 `senderId`）；（c）本地 `.env` 仍是 `pc` + `local`（切手机出声需真实 `VOLC_TTS_API_KEY`，当前为占位值）；（d）`docker`/`.gitattributes` 仍缺，CRLF 坑已在云端实际咬人一次。

71. ⚠️ 独立弹幕监控 + 一个真 bug 修复 + 一个未决诊断（2026-09-16 夜）：用户要求「开一个单独的弹幕监控」（不绑定开播场次），并称「链接我填写在 app 中了」。**① 独立监控已具备**：`server/scripts/danmaku-collect-smoke.ts`（`npm run collect:smoke -- "<分享链接>" <秒数>`）与 `server/scripts/_frame-probe.ts`（逐帧 + 方法普查 + ack 计数）都不依赖场次，贴链接即监听。**② 修掉一个真 bug（影响真实使用）**：`ttwid` 此前**只在展开短链那一步取到**；用户若直接给**完整链接 `live.douyin.com/<房间号>`** 或**纯房间号**，linkResolver 第一遍静态就解析成功、**根本不发那次网络请求** → 拿不到 ttwid → wss 握手被回 `Unexpected server response: 200`（对照实验实测复现）。修法：新增 `createDouyinTtwidFetcher(roomRef)`（`collectors/shortLinkExpander.ts`，只访问公开直播页取它下发的 Set-Cookie），`liveCollector.resolveRoom` 收敛为 `withTtwid()`——短链路径用解析带回的、其余路径现取一次；`danmaku-collect-smoke.ts` 同步回落。修复后完整链接路径 `status: connected`。新增 2 例离线测试（注入替身 fetch），`short_link_expander.test.ts` 15/15。**③ 未决诊断：收不到观众弹幕。** 在用户直播间（`7686117195721837352`，当时正在直播）连续监听两轮、并做公开房间 `369324308707` 对照，结论一致 —— **ack 路径正常**（`needAck 帧 1 ｜ 已回 ack 1`），**帧#1 一次性推完全部房间状态，其后 24 帧全是 94 字节心跳 ack，再无任何内容事件**；方法普查只有 `WebcastRoomDataSyncMessage` / `WebcastResidentGuestMessage` / `WebcastRoomStatsMessage` 等房间元数据，**无 `WebcastChatMessage`（观众弹幕）、无 `WebcastMemberMessage`（有人进场）、无 `WebcastLikeMessage` / `WebcastGiftMessage`**；且服务端下发过 `WebcastLowPcuGuideMessage`（低人气引导），抖音自身把该房间判定为低人气。**下一步判据（需用户配合，很轻）**：让朋友或小号**点进直播间**（不必发言）——「进场」会触发 `WebcastMemberMessage`；若连进场都收不到，则是订阅问题（需查 ack 帧格式 / identity 参数）；若收到了，则此前只是真的没人说话。**遗留**：（a）上述判据待验证；（b）R10 防自嗨仍未做；（c）`UserUniqueId` 仍借分享者 id；（d）含密钥的临时部署脚本已即时删除（`outputs/*.sh`），远程部署统一改为「本地写 .sh → scp → bash」，不再内联长命令（本轮在多层引号上栽了三次）。

72. ✅ 未决诊断闭环：采集链路在真实公共直播间全通（2026-09-16 深夜）：承第 71 条「收不到观众弹幕」。用户提供公共直播间【黄豆豆豆豆豆】（`v.douyin.com/JZptinYC1VQ` → roomId `7686079594273327906`）作**对照实验**。**① 逐帧诊断**（150s）：原始帧 243 / 解出消息 397；方法普查含 **`WebcastChatMessage` ×12**、`WebcastMemberMessage` ×209、`WebcastLikeMessage` ×37、`WebcastSocialMessage` ×8、`WebcastGiftSortMessage` ×3、`WebcastInRoomBannerMessage` ×39；**ack 213 次 needAck 全部回齐（213/213）**。**② 产品链路**（`npm run collect:smoke -- "<链接>" 70`）：**`eventCount 133`、`invalidEvents 0`**、`status connected`，真实弹幕解码逐条可见 —— `软糖酱：豆豆健康就行`、`陈氏果业柚巡（振兴路店）：五个月不立耳怎么办`、`宝马X7：萨摩主播在跟前…`，另有大量 enter / like 事件。**结论：采集链路（解析 → 签名 → ttwid → wss 101 → 解码 → 适配器 onEvent）完全正常；此前用户自己房间「零事件」的原因是「房间里确实没有别的观众说话」** —— 佐证是该房间当时收到过 `WebcastLowPcuGuideMessage`（低人气引导），**抖音自身即判定该房间人气极低**。**③ 方法论结论（写进 §12.1）**：**对照实验是唯一能把「没人说话」与「订阅坏了」分开的手段**。没有它，前面十几分钟「连接正常 / ack 正常 / 只有心跳」的观测极易被误读成故障，进而去改本来没坏的东西。**遗留**：（a）用户房间的真弹幕仍需真实观众，验证方式 = 监听期间让一人**进**直播间（进场必触发 `WebcastMemberMessage`）；（b）R10 防自嗨仍未做；（c）「落库 → 引擎 → TTS → 手机出声」这一段的**真机端到端**仍未跑（服务端侧 R2/R3 已有离线用例覆盖）。

73. ✅ 架构讨论 + R13 采集保活落地（2026-09-16 深夜）：用户就反编译竞品提三个问题（弹幕怎么解 / 采集器要不要上服务器 / 要不要别的采集程序），讨论后拍板「按推荐来」，并纠正我一个误判 ——「**保活是我们需要的**」。**① 反编译实证**（`source/beautified/app-service.js`）：竞品 `getRoomIdByShareCode`（`redirect:'manual'` 拿 302 的 cookies 当 ttwid + 正则 `/reflow/(\d+)/` 取房间号）→ `getWssUrl`（AIOBS SignWss，**ApiKey 硬编码在客户端**、**UserUniqueId 写死一个数字**）→ `connectWebSocket`（header 带 `Cookie: ttwid`）→ `PushFrame.decode → ungzip → Response.decode → needAck 则 doAck → switch(WebcastChatMessage / Like / Member / Social / RoomUserSeq)`。**结论：竞品没有自己「解决」弹幕问题，它把最难的一环（服务端签名）买了 —— 与我们完全同构**；把 ApiKey 硬编码正是它被解包提取的原因。**② 我的误判并更正**：我说「保活是客户端省电的事、我们跑服务端白赚」—— 错。竞品 `onclose` 是 **10s 无限重连** + `startCSystemTimer(30,…)` **30s 看门狗**；而我们 `collectorManager` 的 `openAttempts` **累计计数且连接成功后不复位**，超 3 次即 `finalize(error)` —— **一场直播跑几小时、中途断 3 次就永久停摆**。**③ R13 落地**（`42ca675`）：`maxOpenAttempts` 语义由「累计尝试」改为「**连续失败**」，连接**稳定 ≥ `stableAfterMs`（默认 30s）即清零**（秒断抖振仍计入失败，避免「连上就掉」被无限重试打满）；`liveCollector` 加 **30s 看门狗**（对齐竞品），只扫「**武装中**」的绑定，`suspend`（直播结束）先解除武装 —— 否则看门狗会把已结束场次又拉起来白烧连接。终止文案**保留根因**（`连续 N 次重连失败，终止监听（最后错误：…）`），避免排障时丢掉真正原因。新增 4 例验收测试（稳定可无限重连 / 不稳定仍被有界终止 / error 后被看门狗拉起 / 挂起的不被拉起）。**④ 验证**：`tsc` / `eslint` 0；服务端 **39 文件 / 418 用例：417 passed / 1 skipped / 0 failed**，**连续 3 次复跑均干净**（期间曾出现**一次** 3 例偶发失败，未能复现，如实记录、未定位）。**⑤ 票表**：新增 §13（R13~R17）—— R14 `UserUniqueId` 固定为自身身份 / R15 补 `WebcastSocialMessage`（关注；我们已有 follow 氛围语却无数据源）/ R16 监听源与场次解耦（用户本次「单独的弹幕监控」诉求的正解）/ R17 绑定表脱离内存；**D5 `helperAdapter` 先不接线**（需一台电脑、与「无电脑商家」冲突，留作合规备选出口）、**不做客户端采集**（Key 必泄）。**遗留**：R14/R15/R16/R17 未开工；那 3 例偶发失败未定位。

74. 🐛 修用户报障「粘贴链接有时候会失效」+ R14/R15/R16/R16b 落地（2026-09-17 凌晨）：**① 缺陷根因（真 bug）**：`extractCandidateUrls` 的 URL 正则用的是**黑名单**「排除空白与中文标点」——而**汉字不在排除集里**。于是当 URL **后紧贴中文**时（很多平台分享模板就是这种排版，如 `.../AbC123/复制此链接打开抖音`），汉字会被一起吞进 URL，解析必然失败。**表现为「有时候」失效：取决于那条分享文案 URL 后面有没有空格**（用户自己那条恰好有空格，所以一直没暴露）。用一批真实分享形态做探针复现后确认。**修法**：黑名单换成 **RFC 3986 白名单**（`[A-Za-z0-9-._~:/?#[]@!$&'()*+,;=%]`，短链码只含 `[A-Za-z0-9_-]`，白名单足够且不可能误吞汉字）；同时把**末尾标点剥离**从「只剥中文标点」补齐到**中英文都剥**（此前 `https://v.douyin.com/x/.` 的尾点会被带进短链展开请求）。**验证**：新增 3 例回归测试；并做**端到端回归** —— 用用户真链接构造「中文紧贴」形态，修复前必挂、修复后 `✅ roomRef=7686117195721837352 ttwid=已取到`。**② 同批落地三张票**：R14 `UserUniqueId` 固定为自身身份（不再借分享者 id；换身份后真连 `status: connected` 已验证）；R15 补 `WebcastSocialMessage`（关注）解码（`decodeDouyinSocial` 早已写好只是没人调用；注意 `follow`→氛围语的接线仍是后续项）；R16 监听源与场次解耦（服务端 `startMonitor/stopMonitor/monitorEvents/listMonitors` + `routes/danmakuWatch.ts` 四端点，事件只进内存环形缓冲**不落库**）+ R16b App 侧（首页「AI 直播」区入口卡 + `DanmakuWatchPage`：贴链接 / 2s 增量轮询 / 停止）。**③ 验证**：服务端 `tsc`/`eslint` 0、**39 文件 / 427 用例：426 passed / 1 skipped / 0 failed**；App `dart analyze` 0、**flutter test 164/164**；服务端与本批已部署云端（`/api/danmaku-watch` 四端点 401 存在、health 200）。**④ 又改了既有测试两处（均为有意行为变更）**：`WebcastSocialMessage` 从「未建模方法」变为已建模（改用真正未建模的 `WebcastRoomStatsMessage`）；`eventToIngestInput` 断言补采集字段（更早批次）。**遗留**：R17 绑定表脱离内存；`follow`→氛围语接线；App 侧新增页面**尚未装机**（用户手机 USB 当时未连）。

75. ✅ 循环台本三连（R18 放开字数 / R19 分段份数可见 / R20 条间间隔默认 0s，2026-09-17）：用户提「问题2」三条要求。**① R18 = 原计划 A5-2（一直标着未开工）**：`MAX_LOOP_ITEM_TEXT_LENGTH` 200 → **2000**，并把语义写清 —— 它是**宽松安全上限**（拦模型异常 / 防滥用），**不是业务字数限制**；用户话术**不删字、不截断、不拒绝保存**。长文本合成由 A5-1 的分段合成承接（按标点切段 → 逐段合成 → 拼回单段音频）。**② R19 分段份数可见**：新增 `POST /api/tts/segment-preview`（`routes/tts.ts`）—— 直接复用合成链路自己的 `splitTtsSegments`（**不在客户端另写一份，两份实现必漂移**），回 `{ maxCharsPerRequest, charCount, segmentCount, segments[{index,chars,text}] }`；台本详情每条回带 `ttsSegmentCount`。**③ R20 条间间隔默认 0s**：`DEFAULT_ITEM_GAP_SECONDS` 2 → **0**（用户拍板「循环话本播放期间间隔默认 0s」；2026-09-11 曾由 6s 收到 2s，本次在原方向再压到 0），示例台本 16 处同步、新增迁移 `20260917_v12_loop_gap_zero.sql`（只刷等于旧默认 2 的行，商家手设 1s/3s 保留；**云端 UPDATE 25 行**）。**④ 流畅性**：A5-1 已保证「分段后拼回单段、句中不停顿」；R19 的预览把**断点**也暴露出来（测试断言首段必须以句末标点收尾），使「断点是否落在标点上」可自检。**⚠️ 听感本身只能人耳验收，我做不了。** **⑤ 又改了既有测试两处（有意行为变更）**：`loopScripts.test.ts` 的「201 字必须 400」改写为「201 字必须 201 成功 + 2001 字才 400」；`loopCaster.test.ts` 补两条 R20 用例（默认值为 0 / 条间不产生睡眠只剩轮间休息）。**⑥ 自曝一处失误**：R18/R19/R20 那一提交**lint 未过就提交了** —— 我只 lint 了改动的两个文件，漏掉 `linkResolver.ts` 里一个不必要的 `\[` 转义（上一批引入）。已用 `b4aca0a` 修复并复跑全量。**教训：提交前 lint 必须跑全仓，不能只跑改动文件。** **⑦ 验证**：服务端 `tsc`/`eslint` 0、**39 文件 / 431 用例：430 passed / 1 skipped / 0 failed**；本批已部署云端（v12 迁移 UPDATE 25、`/api/tts/segment-preview` 401 存在、health 200）。**遗留**：（a）**App 侧尚未展示份数**（服务端已就位，编辑器/台本库还没显示 `ttsSegmentCount`）；（b）R17 绑定表脱离内存；（c）`follow`→氛围语接线；（d）手机 USB 未连，R16b 监控页仍未装机。

76. ✅ R19 App 侧 + **真机装机与验收**（2026-09-17 凌晨）：用户插回手机。**① App 侧 R19 落地**：`LoopScriptItem` 增 `ttsSegmentCount`；编辑器单条台词行展示 **「台词 N 字 · 合成 M 段」** —— 服务端已回带的直接用，草稿/新增条目才**防抖 400ms 调预览接口**（客户端不另写切段实现，避免两份实现漂移）。**② 同步放开了客户端残留的两处 200 字上限**（`TextField.maxLength: 200` 与保存前本地校验）——**不改的话服务端虽然放开了，手机端仍存不了长话术**；顺带修正 **3 处「默认 2 秒」过期文案**（默认已改 0s）。**③ 验证**：`dart analyze` 0；**flutter test 166/166**（新增 2 例：长话术展示份数 / 服务端已带份数时不多打一次预览）。**④ 真机装机与验收**：APK 54.3MB 装到华为 ELS-AN10（`versionCode=95`，`lastUpdateTime=2026-09-17 01:17:55`）；**截图确认**首页「AI 直播」区出现「弹幕监控」入口卡（副标题「贴个直播链接就能看弹幕流水（不用开播）」），点击后 **「弹幕监控」页正常打开**（说明卡 + 链接输入 + 开始监控 + 弹幕流水空态）。**R16b 至此真机验收通过。** **⑤ 归档**：`outputs/starvoice-app-release-cloud-r19.apk` + 两张验收截图。**遗留**：R17 绑定表脱离内存；`follow`→氛围语接线；长话术**听感**仍需人耳验收（我只验证了份数与断点，听不了）。

77. ✅ 直播设置六项 App 侧收口（R23 / R25 / R24 服务端）+ ⚠️ 一次数据库事故（2026-09-17）：**① R23 账号级直播设置页**：「我的」→「智能回复与话术」——回复开关 / **六档频次（不回复 · 1 · 5 · 10 · 20 秒 · 自定义 1~60）** 完全对标竞品 `replyOptions` / 补充知识 / 违禁词 / 默认语速滑块；上下限从服务端 `/limits` 拿，前端不硬编码。**② R25 氛围语页**：五类（欢迎 / 关注 / 点赞 / 报时 / 自定义暖场）模板 + 每类插播频次 + **一键补齐推荐模板**（服务端从 M10 起就完整，App 里长期 **0 处** —— 本轮补上，顺带把用户六项里的「欢迎语 / 关注语」落地）。**③ R24 回复台账（服务端）**：新建 `services/replyLedger.ts`（**内存环形缓冲**，单场 50 条）——原因是回复此前**完全没留痕**，商家根本看不到 AI 说了什么；开播清空、结束保留；`/api/lives/:id/monitor` 回带 `recentReplies`（最新在前）。选内存而非落库：这是**可观测**需求不是对账需求，落库要多一张表与一套保留策略。**④ ⚠️ 数据库事故与教训**：本地 PostgreSQL 于 15:11 **被非正常终止**（日志 `database system was interrupted` / `not properly shut down`）。恢复后全量跑出 **46 个失败**，根因 `column "auto_end_minutes" of relation "lives" does not exist` —— **崩溃把 WAL 尾部丢了，v11 / v12 / v13 三个迁移全部回滚**（`live_danmaku.platform` 也没了）。**因为三个迁移当初都写成幂等（`IF NOT EXISTS`），重跑即恢复**，无需手工重建表；重跑后 **42 文件 / 454 用例：453 passed / 1 skipped / 0 failed**。**教训：迁移必须幂等，而且不能假定「跑过一次就一直在」——异常关机会把近期 DDL 一起带走。** **⑤ 另一条教训**：那轮 46 失败之前，我差点把「175 个用例被跳过」当成正常输出 —— 是**跳过数骤然变大**这个信号让我回头查数据库的。**测试的 skip 数也是信号，不能只看 passed。** **⑥ 验证**：服务端 `tsc`/`eslint` 0、42 文件 453 passed；App `dart analyze` 0、**flutter test 170/170**；APK 54.4MB 已归档 `outputs/starvoice-app-release-cloud-r23.apk`（**含 R23，手机 USB 未连未装**；R25 的包尚未打）。**遗留**：（a）**R24 的 App 侧未做**（监控页还没展示 `recentReplies`）；（b）R25 的新包未打包未装机；（c）R17 绑定表脱离内存；（d）`follow`→氛围语接线；（e）长话术听感仍需人耳验收。

78. ✅ 固定回复（第 1 层）+ 可靠性审计收口（2026-09-17）：用户问「是否可以拆解词做固定回复，比如『多少钱？』；话本生成后自动拆解出对应的」，讨论后拍板六项（D1 第 1 层可自动生效 / D2 第 2 层必须人工确认 / D3 命中不走 AI / D4 问法 3~8 个 / D5 短答案 / D6 与氛围语分开建表），全部按推荐执行。**① 先纠正了方向**：我们的 `productSnapshot` 已是**已知键的结构化数据**（`name/package/price/sellingPoints`）—— 「多少钱」的答案**就是 `price` 字段本身**，根本不需要 AI 拆解。据此分三层：结构化直答（零 AI，可自动生效）/ 正文拆解（需 LLM，**必须人工确认**——LLM 会编，编错了播出去比不答更糟）/ 商家手写。**② 竞品对标**：对反编译产物全量搜 `关键词/固定回复/自定义回复/keyword/FAQ` —— **0 命中**，它全走服务端 LLM。**这条对我们是差异化。** **③ R32 落地**（`services/faq.ts`）：**纯函数、零存储** —— 快照就在场次绑定的脚本上、每次回复都已加载，做成无状态就**永远不会和快照漂移**（改了价格答案立刻跟着改，不需要同步任务）。四组问法（每组 3~8 个）、答案裁剪 60 字、**高置信门槛**（弹幕 >30 字不算命中 —— 「上次那个多少钱的套餐我买过了」不该回一句报价）。**④ R33 落地**：引擎 FAQ 优先 → 命中即回（`source: 'faq'`，**一次 DeepSeek 都不调**）；未命中回落 AI 并把固定回复口径一并给它（避免「固定说 99、AI 说 99 起」）；**固定答案自身也要过合规两道闸**（商家字段也可能含敏感词，命中就回落 AI 换说法）。**⑤ 测试抓到一个真缺口**：「价格」是最高频问法之一，我最初只写了「售价/价位」—— 单测立刻挂了，改服务不改测试。**⑥ 验证**：服务端 `tsc`/`eslint` 0、**43 文件 / 466 用例：465 passed / 1 skipped / 0 failed**；App `dart analyze` 0、**flutter test 172/172**；已部署云端（health 200）、APK 54.5MB 归档 `outputs/starvoice-app-release-cloud-r33.apk`。**⑦ 顺带**：审计出**严重频控竞态**（并发 10 条 → 10 次 AI 调用，已实测）等 8 项问题，写在 `docs/SMART-REPLY-AUDIT.md`，修复票 R28~R31 **尚未开工**。**⚠️ 在 R28 修掉前，不建议在热闹直播间无人值守跑。** **遗留**：（a）R28~R31（频控竞态 / 回收 / 可观测 / 重试）；（b）R34/R35（第 2 层拆解 + App FAQ 管理页）；（c）手机 USB 未连，**R23/R24/R25/R32/R33 的包都还没装机**；（d）R17 绑定表脱离内存；（e）`follow`→氛围语接线。

79. ✅ R28/R29/R30 可靠性修复（2026-09-17）：承第 78 条审计出的问题，用户拍板「修改」。**① R28 频控竞态（🔴 最严重那条）已修**：新增 `SerialByKey` —— **每场次一条串行链**，把「读时间戳 → await 生成数秒 → 才写时间戳」这个 check-then-act 从根上串行化；顺带把「同一场次并发调 AI 数」压到 1。**用当初证明 bug 的同一个探针复验：并发 10 条从 10 次 AI 调用 + 10 条回复，降到 1 次 + 1 条。** 串行粒度是**场次**不是全局（不同场次互不阻塞，有单测）。**② R29 频控记账回收**：新增 `forgetLive(liveId)`，由结束直播的收尾路径（`finishLive`）调用清本场；另加 **500 条上界安全网**兜异常路径（进程没走到收尾时也不会无界增长）。`SerialByKey` 自身在链尾自动删 key。**③ R30 可观测**：`subscribe` 的 `.catch(() => undefined)` 改为**落日志**（带 liveId）—— 引擎是 fire-and-forget 调的，之前失败**完全没有痕迹**。**④ 回归防护**：`interaction_engine.test.ts` 新增 3 条（并发 10 条只 1 次 AI 调用 / 两场次各自并发互不拖累 / `forgetLive` 清完立刻可再回）。**⑤ 验证**：服务端 `tsc`/`eslint` 0、**43 文件 / 469 用例：468 passed / 1 skipped / 0 failed**；已部署云端（备份 `162006`，云端源码已含 `SerialByKey`，health 200）。**⑥ 审计文档同步更新**：`docs/SMART-REPLY-AUDIT.md` 加了「修复记录」表 —— §1、§2.1、§2.3 标 ✅，其余（§2.2/2.4/2.5/2.6/2.7/2.8）仍 ⬜。**⚠️ 现在「并发下频控失效」已不存在**；但 §2.4（无重试）/§2.5（出声队列无上限）建议长时间无人值守前处理。**遗留**：（a）R31 有限重试 + 队列上限；（b）R34/R35（第 2 层拆解 + App FAQ 管理页）；（c）**手机 USB 始终未连，R23/R24/R25/R32/R33 五个包全都没装机**；（d）R17 绑定表脱离内存；（e）`follow`→氛围语接线。

80. ✅ 真机测试首次完整跑通 + 抓出并修掉两个真 bug（2026-09-17）：用户要求「部署到云端 然后真机测试」。**① 装机**：adb install -r 成功（lastUpdateTime=16:21:51）。**② 真机逐项验收（截图 + 回云端查库）**：R23 账号级设置——入口/页面/六档频次弹窗/违禁词/语速滑块全部正常，改完保存后回查云端得到 `enabled=true | interval=20 | banned=test1 | speechRate=30`，**全部落库**；R25 氛围语——入口在位、「一键补齐推荐模板」补出五类、频次改「不插播」后云端 `welcome interval = 0`。**③ 🐛 BUG A（云端数据缺失）**：氛围语页报 `relation "atmosphere_settings" does not exist`——**M10 的 v06 迁移在云端从来没跑过**。根因是**我的部署脚本只重跑了我记得的 v11/v12/v13**，没有「跑全部待执行迁移」这一步。修法：写一个**按文件名顺序全量跑 drizzle/*.sql** 的脚本（13 个全幂等，且 v08/v09/v10 这些数据迁移是**自收敛**的——跑第二遍不再匹配任何行），云端 13/13 OK，`atmosphere_settings` 0→1。**这条要变成部署的标准步骤。** **④ 🐛 BUG B（客户端形状写错，我的错）**：表补上后页面仍报「网络连接失败」，但服务端两个接口**都返回 200**。真相是**我把返回形状写错了**——服务端 GET 回**裸数组**、POST/PUT 回**裸对象**，我却按 `{templates:[...]}` / `{template:{...}}` 解析；PUT 还漏传 `category`（服务端是整体替换口径）。**更值得记的是：假后端当初照着我错误的假设写，所以 widget 测试全绿**——单测掩盖了真机必挂的形状错误。修法：客户端对齐真实形状 + 补 `category` + 频次更新后**保留本地 rule**（服务端 PUT 不回带 rule）+ **假后端改成如实镜像真服务端**。**⑤ 方法论**：真机测试期间用 journalctl 查服务端日志，确认「App 到底有没有发出那个请求」——这一步把「点没点上」和「客户端有没有 bug」彻底分开了（日志显示 App 确实发了 3 次 PUT，问题随即定位到我的点击坐标而非功能）。**⑥ 验证**：App dart analyze 0、flutter test 172/172。**截图存证**：outputs/rt-01..rt-19*.png。**遗留**：（a）R31 重试+队列上限；（b）R34/R35；（c）~~一处数据瑕疵：云端 welcome 类有两条模板、App 只编辑第一条~~ **【已更正 · 这条是误报】** —— 重新按 user 过滤查库后确认：两条 welcome **属于不同用户**（本账号 9ccc3e5a 有 5 条、另一账号 c52fa28d 有 1 条）。我最初的 psql **漏了 user 过滤**而接口是按 user 过滤的，所以 **App 显示 1 条本来就是对的**。教训：**自查数据时也必须带上租户过滤**，否则会把「别人的数据」当成「自己的瑕疵」；（d）R17；（e）follow→氛围语接线。

81. ✅ 产品三轮讨论定案 + R41/R45 落地（2026-09-17）：用户提「① 互动会阻碍话术播报吗 ② 弹幕过多怎么处理」，要求**按产品逻辑讨论**，三轮后定调：「**产品是直播卖货类的；节奏一定要好；固定的台本在；但一定要避免太多无效弹幕**」，并拍板「**闲聊算无效**」、其余按推荐。**① 第一轮（工程）**：查出**台本会被互动无限饿死**——`loopCaster` 每句前的 `while (isBusy) sleep()` **没有超时、没有配额、没有保底**；临界条件是「回复音频时长 > 回复间隔」，而默认语速 -10 ≈ 4.7 字/秒、一条 20~40 字回复 ≈ 4~9 秒，**默认 5 秒/次已贴着临界线**，而 App 里还有「1 秒/次」这一档（选它等于一键关掉自己的台本）。**② 第二轮（产品）修正了我自己的夸大**：我说「台本被饿死 = 功能失效」——**推理有漏洞**，因为**弹幕回复的内容本身就是商品信息**（「咱家 99 元」对旁听者不是噪音）。真正的危害是 **重复（复读机感，直接掉人）/ 碎片化（新观众拼不出完整认知）/ 时长失控（提示词要短句，但 `max_tokens:160` 折算可达 21~55 秒）**。目标因此从「保底」改成「**节奏**」。**③ 第三轮（定调后）**查出一个必须马上说的事实：无效弹幕现在**既花钱又占位**——判定顺序是「时间窗限流 → 固定回复 → LLM 判断」，于是灌水 ① 抢走名额把真问题挤掉（真问题直接消失、永不再来）；② 照样触发一次 DeepSeek 调用、读完返回 NONE，**花钱买了一句「不说话」**。**④ R41**：新增 `services/danmakuQuality.ts`（纯函数分类 question/need/greeting/smalltalk/spam），在 `handle` 里**放最前面**（频控之前），无效的直接 `INVALID_DANMAKU` —— 不占名额、不花钱。**单测抓到我漏了中文最常见的疑问标记**：「有优惠吗」的「吗」不在疑问词表里、被误归 need → 补疑问助词规则（改实现，不改测试）。**⑤ R45**：新增 `services/interactionStats.ts`（收到 / 已回 / **因频次漏掉** / 分类分布），`/monitor` 回带，开播清空、关停销毁——这是判断「频次是不是设太紧」的唯一依据（用户拍板「留计数不留内容」）。**⑥ 又一次数据库事故**：本地 PostgreSQL **今天第二次被非正常终止**，全量跑出 **175 个跳过** —— 靠上次记下的「**skip 数也是信号**」立刻发现；拉起后**重跑全部 13 个迁移**（幂等救场）并复核，全量 44 文件 / 481 用例：**480 passed / 1 skipped / 0 failed**。**⑦ 部署**：把「**全量跑 drizzle/*.sql**」正式写进部署脚本（上次云端缺 v06 就是没这一步），云端 13/13 OK、health 200、新模块就位。**遗留**：（a）**R42 节奏规则**（一个空档最多一条 + 轮次完整性）未做；（b）R43 互动队列+优先级 / R44 进场→欢迎语 / **R45 的 App 展示**（服务端已回带，界面没显示）；（c）R31 重试+队列上限；（d）R34/R35。

82. ✅ R42 节奏规则落地（2026-09-17）：承第 81 条遗留，用户定调「节奏一定要好」的直接落点。**① 改了什么**：新增 `services/pendingReplies.ts`（每场次有界队列，`MAX_PENDING_REPLIES=10`，满了丢最旧）；`interactionEngine` 的出口改为「**台本在跑 → 入队；台本没跑 → 直接出声**」（未绑台本时不至于把回复憋死在队列里）；`loopCaster` 新增**空档插播回复**，位置在氛围语**之前**（对齐代码里原本就写明的优先级：回复 > 氛围语 > 台本句），且**一个空档只给一次插播机会**（放了回复就不再插氛围语）。**② 为什么这样修**：旧机制是「回复直接抢播 + 台本每句前 `while(isBusy)` 等链路排空」，而那个 `while` **没有超时、没有配额**；当「回复音频时长 > 回复间隔」时链路永远排不空、台本被无限让位（默认 5 秒/次已贴临界线，App 里还有「1 秒/次」一档）。改成队列后：**节奏有硬保障**（无论弹幕多少，台本按轮推进）、**轮次完整**（一轮每句都播得到）、**队列有上界**。**③ 测试**：新增 2 条 caster 用例 —— 「**一个空档最多放一条回复**（积压 4 条也一样）」`['台本句A。','回复1','台本句A。','回复2','台本句A。']`；「**回复优先于氛围语**（一个空档只给一次机会）」`['台本句A。','回复1','台本句A。','欢迎语']`。另新增 `pendingReplies.test.ts` 4 条（FIFO / 场次隔离 / 上界丢最旧 / 清场）。**其中第二条我第一次写错了停止条件**（第 2 句台本就 stop，氛围语还没轮到）——是**测试自身逻辑错**，改测试不改实现。**④ 顺带**：`/monitor` 回带 `pendingReplies`（还有几条在队列里等着放），让积压可见。**⑤ 验证**：`tsc`/`eslint` 0；**45 文件 / 487 用例：486 passed / 1 skipped / 0 failed**；已部署云端（备份 `170128`、迁移全 OK、health 200、新模块就位）。**遗留**：（a）R43 互动队列优先级（当前 FIFO，未按 question > need 分级）；（b）R44 进场→欢迎语；（c）**R45 的 App 展示**（服务端已回带 interactionStats + pendingReplies，界面还没显示）；（d）R31 重试+队列上限；（e）R34/R35。

83. ✅ R45 App 侧落地（2026-09-17）：承第 82 条遗留。**① 做了什么**：`LiveMonitor` 模型增 `interactionStats` + `pendingReplies`（新增 `InteractionStats` 值对象，含 `valid`/`filtered`/`throttledHeavy` 派生量）；监控页在「AI 回复」之上新增**「互动概况」卡** —— 三个大数（**有效提问 / 已回复 / 因频次漏掉**）+ 一行「挡掉无效弹幕 N 条（灌水 · 闲聊 · 问候）」+ 排队积压，并在**频次偏紧时给出可执行建议**（判据 `throttled >= 5 && throttled > replied`，文案直接指路「我的 → 智能回复与话术」）。**② 为什么这是产品上必须的**：商家此前**完全不知道**自己漏了多少问题，只会觉得「今天 AI 怎么不搭理人」；这一卡是判断「频次设太紧还是太松」的**唯一依据**，也是 R41（前置过滤）效果的唯一判据。**③ 测试**：新增 `interaction_stats_test.dart` 3 例 —— 正常展示（含 86 条被挡的分布）/ 频次偏紧时出建议 + 排队积压可见 / 频次正常时**不打扰**（不出建议）。**④ 验证**：`dart analyze` 0；**flutter test 176/176**；装机成功。**⑤ 真机验证的诚实说明**：云端接口 `GET /api/lives/:id/monitor` 已确认回带 `interactionStats` 与 `pendingReplies`（实测返回结构正确）；但**这一卡在真机上没跑到有数据的画面** —— 需要一场**进行中的直播**，而当前账号 13 个场次全是「已结束」（已结束态的卡片点不开详情）。所以 UI 由 widget 测试覆盖、接口由真机实调覆盖，**「真机上看真实数字」这一步留到下次开播时做**。**遗留**：（a）R43 队列优先级（现在 FIFO）；（b）R44 进场→欢迎语；（c）R31 重试+队列上限；（d）R34/R35；（e）**开一场真实直播做端到端验收**（把所有 R41/R42/R45 的成果一次跑通）。
