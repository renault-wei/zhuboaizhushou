# AGENTS.md — Codex / AI 编码助手工作约定

> 本文件是 AI 编码助手（Codex CLI）在本仓库工作时的强制约定。
> 修改代码前必读。

## 项目背景（30 秒版）

「星辰语音 App」面向本地团购商家的 AI 直播工具：
商家录 3 分钟声音 → AI 克隆音色 → AI 生成团购话术 → 一键实景无人直播卖团购券。
MVP 范围 = 24 个 P0 功能 + v0.2 双模式增量 G1~G7（均见 `docs/DEV-SPRINTS.md`）
+ v0.3 公司端商业化 M5~M8（见 `docs/console-roadmap.md` §6），**超出范围的需求一律拒绝实现**。

## 目录职责

- `app/`：Flutter 客户端。状态管理用 Riverpod，路由用 go_router，网络层用 dio。
- `server/`：Node.js + Fastify + PostgreSQL。MVP 严禁引入微服务/消息队列/Docker 编排。
- `admin/`：管理后台 Web（React + Vite），只做内部运营用，不追求视觉。

## 硬性规则

1. **合规红线（违反即 bug）**
   - 直播画面必须叠加「AI 智能直播」角标，且**不提供任何关闭入口**
   - 声音克隆前必须完成《声音授权协议》签署并存档（PDF + 时间戳）
   - 话术生成后、开播前必须跑敏感词扫描，命中拦截级词直接阻断
2. **成本红线**：所有第三方 AI 调用必须先查该商家剩余额度，超额直接拒绝并返回引导续费文案
3. **密钥管理**：API Key 一律走环境变量 / `.env`（已在 .gitignore），**任何代码、注释、文档中不得硬编码密钥**
4. **不做的功能**（v0.3 口径）：数字人、多平台推流、多门店、托管模式实时弹幕互动；现场互动（模式 B）的实时弹幕互动按 G3~G6 增量提供。
   商业化口径（v0.3）：公司端按「服务端扫码直充 + 卡密批次核销 + 服务端开关下发」落地（M5~M8，见 `docs/console-roadmap.md`），
   预充时长档位由服务端配置下发（解除 v0.2「多档套餐」排除）；真实支付凭证未就绪前一律 mock 演示单 + 运营人工确权，
   **不真调第三方支付、支付凭证不硬编码**（沿用 .env 机制）
5. 数据库变更写迁移文件（server/drizzle 或手写 SQL 迁移），不直接改表

## 代码风格

- TypeScript / Dart 均开启严格模式
- 提交前保证 `npm run lint` / `dart analyze` 无 error（warning 可容忍）
- 注释用中文，变量名用英文
- 单个 PR/任务改动控制在 500 行以内，大了拆分

## 依赖三方服务（均为 OpenAI 兼容或 REST）

| 服务 | 用途 | 配置前缀 |
|---|---|---|
| DeepSeek | 话术生成 | `DEEPSEEK_API_KEY` |
| 阿里云 CosyVoice | 声音克隆+TTS | `COSYVOICE_*` |
| 抖音开放平台 | OAuth + 团购券 + 推流 | `DOUYIN_*` |
| 微信支付 | 订阅支付 | `WXPAY_*` |
| 支付宝（当面付/商家码，M8 接入） | 服务端扫码直充 | `ALIPAY_*`（接入前无需配置） |

## 测试要求

- server：核心接口（登录/克隆/生成/开播/支付回调）必须有集成测试
- app：关键页面写 widget 冒烟测试，不追求覆盖率
- 第三方 API 全部 mock，测试不得真实调用（花钱！）
