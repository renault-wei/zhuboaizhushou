<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 联调后端需要网络访问：debug 清单已带权限，release 包也需要 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 声音克隆录音：T4 需要麦克风权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- 助播保活（M9 手机线）：出声期间以前台服务固定进程优先级，避免切后台/锁屏被冻结 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- R62 保活加固（对照竞品 xcai1618 的清单取证）：
         ① 主动申请电池优化豁免 —— 系统提供的**正规 API**，会弹系统对话框；
            此前我们只写了个「请你自己去设置里找」的引导页，等于连正规手段都没用满。
         ② 开机自启 / 应用被替换后重排保活（需用户先放行自启动，代码绕不过）
         ③ JobScheduler 定时唤醒（BIND_JOB_SERVICE）—— 不受应用定时器被限流影响 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.BIND_JOB_SERVICE"/>
    <!-- R66 关键修复（2026-09-22 真机实测）：
         **Android 的权限设置页只列出「声明了该权限」的应用** ——
         系统「显示在其他应用的上层」（悬浮窗）列表只会列声明 SYSTEM_ALERT_WINDOW 的应用。
         我们此前没有这一条 → 用户在那个页面**根本搜不到本应用** ✗，
         而我们的权限向导第 2 步恰恰把用户带去了那里 ✗（把人送到一个自己不在名单里的页面）。
         竞品 xcai1618 的清单里本来就有这一行，是我抄权限时漏掉的。 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <!-- MVP 联调使用 http://10.0.2.2:3000，需允许明文流量；上线切 https 后可移除 -->
    <!-- R66：应用在系统里的名字。
         此前这里是 `starvoice_app`（= 包名）—— 用户在「设置 → 应用管理 / 应用启动管理 /
         权限管理」里搜「星辰语音」**根本搜不到** ✗（桌面上也只显示被截断的 "starvoice..."）。
         保活那几道闸都得用户手动去系统设置里点，名字对不上等于路都没法走。 -->
    <application
        android:label="星辰语音"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <!-- 助播保活前台服务：mediaPlayback 类型 + 常驻通知，开播期间保持出声不断 -->
        <service
            android:name=".AssistantKeepAliveService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback"/>
        <!-- R62：JobScheduler 保活心跳（对照竞品 CactusJobService） -->
        <service
            android:name=".KeepAliveJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"/>
        <!-- R62：开机 / 应用更新后重排保活（需用户先放行自启动） -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
